# Собранный пуск Reviewer-сессии F4-13 — расчистка хвоста журнала решений

Собран сессией `arch_f4_17_brief` 2026-08-12, блок 9, по требованию Owner. Копируется ЦЕЛИКОМ одним
сообщением и не редактируется: слотов на заполнение нет ни одного, SHA роль берёт сама (ADR-117 §1,
`docs/09_RUNBOOK.md`). Адреса ниже верны на коммите `6b68d9b`. Появились новые коммиты до запуска —
файл пересобирается сессией, а не правится руками.

---

Ты внутри репозитория dom-prorab-ecom-arch, файлы читаешь с диска, curl и SHA не нужны. Первым
действием выполни `git rev-parse HEAD`: команда не отработала или хэш не вернулся — стоп, ты
запущена не там, доложи Owner и дальше не читай.
Режим этой сессии: Reviewer (перечень режимов — CLAUDE.md §Мандат).
Служит: Ф-1 и Ф-2 (`l0-canon/project_goal.md` §2) через снятие долга: тридцать три записи журнала
стоят в статусе `proposed`, то есть по правилу К-15 не применяются, а на них уже опираются
работающие задачи; одна из них, ADR-079, прямо запирает строку 7 очереди (`F4-09`).
Твой промт — `prompts/reviewer.md`, читай его с диска целиком. Раздел «Доступ» в нём — дисциплина
чатового аварийного пути и на этой сессии не действует. Законы — CLAUDE.md. SHA сессии = вывод
`git rev-parse HEAD`, каждая цитата в формате файл@SHA:строки (К-1, К-21).

ЗАДАЧА. Провести заход целостности по хвосту непринятых записей журнала решений и выдать список
находок — предмет строки 6 очереди (`F4-13`). Апрув и смену статусов сессия НЕ делает: решение
принимает Owner пачкой, строки статуса правит применяющая роль (ADR-016, К-15). Патчей ты не
выдаёшь и записей не выпускаешь (`prompts/reviewer.md` §Роль).

ПРЕДМЕТ. Предъяви все адреса ДО чтения обязательного контекста (К-36); не нашёлся хотя бы один —
стоп и доклад Owner:

* `docs/05_ROADMAP.md:85` — строка 6 очереди: состав задачи, брифа не требует, гейта нет
* `docs/02_ADR_LOG.md:4018-4079` — ADR-089: §1 даёт имя `F4-13`, называет состав и ОБЯЗЫВАЕТ
  пересчитать его на момент старта задачи; §6 объясняет, почему сама она вышла `proposed`
* `docs/02_ADR_LOG.md` — ADR-033 §2: третья точка ревью, обязательная перед сменой статуса
  `proposed` → `accepted` у PROCESS-записи, меняющей правила метода; технические PROCESS-записи под
  точку не попадают, сомнение трактуется в пользу ревью
* `docs/01_CONVENTIONS.md` — К-15 (предложенное коммитится и без апрува не применяется) и К-40
  (кто выходит `accepted`, кто `proposed`)
* `docs/02_ADR_INDEX.md` — строка на каждую запись со статусом; индекс и лог обязаны сходиться
* `prompts/reviewer.md` — твой промт целиком
* `reference/2026-08-12_parallel_with_f4_17.md` — разбор очереди: почему эта задача идёт сейчас и
  почему она никого не блокирует

ФАКТУРА НА КОММИТЕ `6b68d9b`, ПЕРЕСЧИТАЙ ЕЁ САМА (ADR-089 §1 требует пересчёта, и это не
формальность — с даты записи перечень вырос вдвое). На указанном коммите записей в статусе
`proposed` — тридцать три: тридцать одна заголовочной формы и две списочной (`ADR-070`,
`ADR-071` — у них строка статуса идёт с отступом, ADR-073 §2). Классы: ARCH — тридцать одна,
PROCESS — две (`ADR-073` и `ADR-117`). Номера от `ADR-069` до `ADR-117`. Расхождение твоего счёта
с этим — находка, а не повод подогнать.

ЧТО УЖЕ РЕШЕНО И НЕ ПЕРЕСПРАШИВАЕТСЯ (ADR-117 §2 — решение, лежащее в репо, называется адресом):

* Имя и место задачи — `F4-13`, строка 6 очереди (ADR-089 §1). Брифа не требует: связка
  Reviewer — Owner — применение, и открывается она пусковой строкой с предметом (К-36).
* Апрув идёт ОДНОЙ ПАЧКОЙ по образцу ADR-044…ADR-050 (ADR-089 §1). Смена статуса — правка одной
  строки, тело записи не трогается никогда (ADR-016).
* Заход Reviewer обязателен перед апрувом PROCESS-записи, меняющей метод (ADR-033 §2). Для
  ARCH-записей эта точка не обязательна — но и не запрещена; объём захода решаешь ты, см. ниже.
* Ты не принимаешь решений по находкам и не пишешь ADR: по каждой находке решение принимает
  Architect или Business отдельной записью (`prompts/reviewer.md` §Роль).
* Класс B (отправка подрядчику · `git push` · правка `l3-external/raw`) мандатом сессии не
  покрывается никогда; перечень живёт в CLAUDE.md §Мандат в единственном экземпляре.

ЧТО НЕ РЕШЕНО И РЕШАЕШЬ ТЫ:

* ОБЪЁМ ЗАХОДА. Обязательны по ADR-033 §2 только две PROCESS-записи. Остальные тридцать одна —
  ARCH, и по букве правила точку ревью не требуют. Решай сама: заход по двум обязательным, по всем
  тридцати трём либо по названному тобой подмножеству с критерием отбора. Критерий назови явно и
  проверяемо, «на глаз» отбором не является (К-28).
* УСТАРЕВШЕЕ ТЕЛО ПРИ СТАТУСЕ `proposed` — главный класс риска, и он тебе отдан целиком. Часть
  записей писалась до решений, которые их обогнали: например ADR-104 §9 объявляет предусловием
  `F4-17` закрытие `OQ-08`, а ADR-134 §1 (`accepted`) этот гейт с задачи снял; ADR-099 §3 закрывает
  перечень задач с брифом пятью, а позже заведены ещё три. Апрув такой записи ратифицирует текст,
  который уже неверен, и тело править нельзя (ADR-016). Проверь предъявленные два случая своим
  прогоном и найди остальные; форму закрытия каждого предложи, но не применяй.
* ФОРМА ВЫХОДА ЗАХОДА. Один список находок на весь хвост либо разбиение на пачки, которые Owner
  апрувит по отдельности. ADR-089 §1 называет «одну пачку» про апрув, а не про твой отчёт, и
  числа пачек не задаёт.
* ПРИЗНАК ПРИЁМКИ ЧИСЛОМ (К-28): какое утвердительное число предъявляет заход, чтобы его выход
  считался полным. Голое «отревьюил» приёмкой не является.

Решение Owner по этим четырём пунктам НЕ получено — принимай их сам и объяви форму прямо; заход
Reviewer записей журнала не выпускает, поэтому вопрос статуса `proposed` к твоему выходу не
относится, а к находкам — относится (ADR-117 §3).

Конец сессии — один session-блок по `docs/04_SESSION_PROTOCOL.md` в описание локального коммита.
Помни про новую форму: `§Подробности для модели` в `docs/00_STATE.md` — индекс, строка на сессию,
прозой туда не пишут; наблюдение без владельца идёт в `docs/10_SCARS.md` номером `Ш-NN`, учётная
строка вопроса — в `l2-registers/open_questions.md` (К-19 и К-23 в редакции ADR-137 и ADR-138).
`git push` не выполняй.
