# Собранный пуск Architect режим B — выпуск брифа F4-17 (половина прогона без 1С и эквайринга)

Собран сессией `arch_rev11_findings` 2026-08-11 по требованию Owner. Копируется ЦЕЛИКОМ одним
сообщением и не редактируется: слотов на заполнение нет ни одного, SHA роль берёт сама
(ADR-117 §1, `docs/09_RUNBOOK.md`). Адреса ниже верны на коммите
`dabad2db3ada2c011c45fabe2994b796965fdfc3`, которым применена запись ADR-134. Появились новые
коммиты до запуска — файл пересобирается сессией, а не правится руками.

---

Ты внутри репозитория dom-prorab-ecom-arch, файлы читаешь с диска, curl и SHA не нужны. Первым
действием выполни `git rev-parse HEAD`: команда не отработала или хэш не вернулся — стоп, ты
запущена не там, доложи Owner и дальше не читай.
Режим этой сессии: Architect режим B (перечень режимов — CLAUDE.md §Мандат).
Служит: Ф-1 и Ф-2 (`l0-canon/project_goal.md` §2) — колонка «чем проверено» регистра гэпов пуста
по всем тридцати шести строкам, и пока она пуста, ни один шаг обоих фактов границы не подтверждён
ничем, кроме документации подрядчика.
Твой промт — `prompts/architect.md`, читай его с диска целиком. Раздел «Доступ» в нём —
дисциплина чатового аварийного пути и на этой сессии не действует. Законы — CLAUDE.md. SHA
сессии = вывод `git rev-parse HEAD`, каждая цитата в формате файл@SHA:строки (К-1, К-21).

ЗАДАЧА. Выпустить `briefs/F4-17.md` по `docs/06_BRIEF_TEMPLATE.md` — бриф на прогон шестнадцати
шагов карты, оставшихся в задаче после сужения ADR-134 §1. Больше эта сессия не делает ничего:
прогон исполняет Executor по брифу, регистр гэпов заполняет он же.

ПРЕДМЕТ. Предъяви все адреса ДО чтения обязательного контекста (К-36); не нашёлся хотя бы
один — стоп и доклад Owner:

* `docs/02_ADR_LOG.md:8383-8455` — ADR-134 (accepted): сужение объёма, перечень шестнадцати шагов
  поимённо, снятие гейта, заведение `F4-23`, и §5 — единственное, что оставлено тебе
* `docs/02_ADR_LOG.md:5511-5600` — ADR-104: форма выхода задачи. §2 — семь полей строки протокола,
  §3 — без внутренних кодов, §4 — файл в `reference/`, §5 — расщепление прогона надвое по стороне,
  §7 — два ограничения, названные до начала прогонов
* `docs/05_ROADMAP.md:84` — строка 5 очереди в новой редакции: объём, форма выхода, «гейта НЕТ»
* `docs/05_ROADMAP.md:96` — строка 17 очереди: `F4-23`, куда ушёл остаток из двадцати шагов
* `l0-canon/order_flow_e2e.md:44-930` — §1 карты, все тридцать шесть шагов с девятью полями каждый;
  поле «Направление» и есть граница объёма
* `l0-canon/order_flow_e2e.md:931-983` — §2, закрытый перечень четырнадцати стадий воронки
* `l0-canon/order_flow_e2e.md:1227` и ниже — §5, матрица мандатов: кто вправе инициировать шаг
* `l2-registers/implementation_gaps.md:1-6` — шапка регистра; колонка «чем проверено нами» — то,
  что прогон заполняет, и её текущее значение по всем строкам «не проверено»
* `l2-registers/open_questions.md:30` — строка `OQ-08` с исправленной колонкой «Блокирует»
* `briefs/F4-22.md` и `briefs/F4-15.md` — два выпущенных брифа как образец формы, не содержания
* `docs/06_BRIEF_TEMPLATE.md` — шаблон, по которому бриф обязан быть собран

ЧТО УЖЕ РЕШЕНО И НЕ ПЕРЕСПРАШИВАЕТСЯ (ADR-117 §2 — решение, лежащее в репо, называется адресом):

* Объём задачи — ровно шестнадцать шагов, названных ADR-134 §2 поимённо: `OF-01`, `OF-02`,
  `OF-05`, `OF-06`, `OF-08`, `OF-11`, `OF-13`, `OF-15`, `OF-19`, `OF-20`, `OF-21`, `OF-22`,
  `OF-24`, `OF-29`, `OF-32`, `OF-36`. Перечень не пересчитывается и не переоткрывается: он
  зафиксирован поимённо именно потому, что подстрочный отбор по «1С» ошибается на `OF-13`.
* Гейт `OQ-08` с этой задачи СНЯТ (ADR-134 §1). Формулировка ADR-104 §9 «бриф выпускается после
  закрытия OQ-08» относилась к несуженной задаче и на эту не действует — стопом она не служит.
* Остаток из двадцати шагов — не твоя задача: он заведён `F4-23` и ждёт закрытия `OQ-08`.
  Требований и вопросов по нему бриф не несёт.
* Форма выхода задана ADR-104 и не переоткрывается: протокол прогона по `OF-NN`, семь полей на
  строку, файл на прогон в `reference/`, без внутренних кодов проекта. Второго слоя учёта нет —
  вердикт живёт в колонке «чем проверено нами» регистра гэпов.
* Два ограничения прогона названы ADR-104 §7 до начала и действуют: прогоны по боевому контуру
  допустимы, пока магазин не запущен, и прекращаются в день запуска; каждый прогон обязан
  оставить след номерами, иначе его нечем сверить.
* Исполнитель — Executor по `prompts/executor.md`. Решений он не принимает: место, где требуемое
  поведение не названо по адресу, есть стоп и Architect, а не повод дописать (норма ADR-132 §2).
* Трассировка к границе обязательна (ADR-052): бриф несёт строку «Служит: Ф-1 и Ф-2».
* Набор файлов на запись обязателен и машиночитаем (К-31).

ЧТО НЕ РЕШЕНО И РЕШАЕШЬ ТЫ:

* Доступность трёх внешних каналов внутри объёма НЕ УСТАНОВЛЕНА ни одной записью репо, и это
  объявлено прямо записью ADR-134 §5. Каналы: Яндекс B2B Delivery API (`OF-20`, `OF-21`,
  `OF-22`); WhatsApp через Wazzup (`OF-05`, `OF-24`, `OF-29`, `OF-36` — половина Owner по разрыву
  Д-13 заведена строкой `F4-21` роудмапа и не закрыта); витрина CMS и оформление заказа на сайте
  (`OF-01`, `OF-13`). По каждому каналу реши сам: шаги входят в объём брифа либо выносятся
  вне-scope с причиной по адресу репо. Предполагать доступность запрещено — «не установлено» не
  равно ни «есть», ни «нет» (К-14); не нашёл записи — это и есть ответ, и он оформляется строкой
  открытого вопроса либо вопросом Owner, а не догадкой.
* Разбивка прогона на файлы: один протокол на все шаги объёма либо по файлу на сценарий
  (доставка / самовывоз / возврат). ADR-104 §4 требует файл на прогон и числа не задаёт.
* Признак приёмки задачи числом (К-28): какое утвердительное число предъявляет Executor, чтобы
  выход считался принятым. Голое «прогнали» приёмкой не является.
* Нужен ли брифу отдельный шаг на порядок безопасности прогона по боевому контуру сверх двух
  ограничений ADR-104 §7 — и если да, чем он проверяется.

Решение Owner по этим четырём пунктам НЕ получено — принимай их сам и объяви форму прямо: запись
выйдет `proposed` по К-40 либо подашь развилку Owner до выпуска брифа; выбор за тобой (ADR-117 §3).

Конец сессии — один session-блок по `docs/04_SESSION_PROTOCOL.md` в описание локального
коммита. `git push` не выполняй.
