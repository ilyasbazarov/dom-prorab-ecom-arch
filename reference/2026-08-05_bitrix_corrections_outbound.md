Здравствуйте. Два уточнения по задачам, которые мы передали 3 августа.

**1. Задача про идемпотентность и порядок вебхуков.** В переданной редакции критерий приёмки
звучал так: «Курсор не влияет на обработчики `oos_at_picking` и `check_printed`». Формулировка
наша и она неточная. Правильно так: курсор статус-канала на эти два обработчика действительно не
влияет, но у каждого из них должен быть свой курсор, работающий по тому же правилу.

Зачем. Повторная нехватка по одному и тому же заказу — легитимный сценарий, он прямо описан в
задании: менеджер решает вопрос, сборка продолжается, нехватка возникает второй раз. При ключе
только по паре «сделка + событие» второе такое событие молча отбрасывается как дубль, и заказ
зависает. Со своим курсором на канале второе событие проходит, потому что версия документа выше.

Просим ответить одной строкой: реализован ли этот критерий буквально, то есть заведён ли курсор
только на статус-канале? Если работа уже сделана — назовите объём и стоимость доработки.

**2. Задача про обработчик события подтверждения заказа.** Отдельно подтверждаем ранее
сказанное: события `order_partial` не будет, статус «Частично» снимается полностью. Ветку
обработчика под него писать не нужно. Если она уже написана — сообщите, мы учтём.

---
Служебное, контрагенту не отправляется.
Статус: к отправке. Основание содержания — ADR-084 §1 и §6, форма доставки решена Owner
2026-08-05 (вариант A). Отправка — необратимая операция класса B, исполнение и фиксация даты за
Owner. Файл не правится после коммита (К-33); новая редакция — новый файл с новой датой.
Связанные записи реестра: P-04, CQ-24.
