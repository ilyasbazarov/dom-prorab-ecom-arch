# FILE: reference/2026-08-10_answer_check_negative_run_rev2.md

# Негативный прогон проверки ответа, редакция 2 (К-34, К-22)

Предмет: `tools/hooks/answer_check.sh` после ADR-123 §1 и §3. SHA сессии `214b79505f708b50f3243e99fa9dbe4c0138d244`.
Редакция 1 — `reference/2026-08-10_answer_check_negative_run.md`, снимок, не правится (К-33).
Прибавилось: четыре признака упаковки смысла (К-42) и расширение лимита блоком развилки.

**Признак проверяется по ВЕТКЕ ОСТАНОВА.** Ниже дословный вывод на девяти намеренно
нарушающих ответах. Два положительных кейса удостоверяют только отсутствие ложного
срабатывания: по К-34 положительная ветка признаком не является.

## Команда

```
LC_ALL=en_US.UTF-8 bash tools/hooks/answer_check.sh --text <файл ответа>; echo "rc=$?"
```

Код 2 — ответ возвращён роли на переписывание. Код 0 — форма и упаковка сошлись.

## Кейсы и дословный вывод

### good — ПОЛОЖИТЕЛЬНАЯ ВЕТКА: шапка на месте, абзацы короткие, перечень лимитом не считается

```
rc=0
```

### fork_ok — ПОЛОЖИТЕЛЬНАЯ ВЕТКА: 36 строк при одной развилке — лимит расширен до 37 (ADR-123 §3)

```
rc=0
```

### no_header — ответ начинается прозой, шапки К-39 нет

```
ОТВЕТ ВОЗВРАЩЁН — нарушений 3 (tools/hooks/answer_check.sh):
  1. [ФОРМА-1] строка 1 шапки не «Сделано:», а «Разобрался с задачей. Ниже подробности того, что происходило по шагам.»
  2. [ФОРМА-1] строка 2 шапки не «Нужно от Owner:», а «Сначала прочитал конвенции, потом поднял ADR-074, потом сверил промты.»
  3. [ФОРМА-1] строка 3 шапки не «Рекомендация:», а «<пусто>»
rc=2
```

### long_para — абзац из четырёх строк — читатель сам собирает вывод

```
ОТВЕТ ВОЗВРАЩЁН — нарушений 1 (tools/hooks/answer_check.sh):
  1. [ФОРМА-4] абзац длиной 4 строк при лимите 3 (К-39 §3), начало: «следует, что правку надо было делать в другом месте.»

Перепиши по К-39 и К-42: шапка из трёх строк, вывод раньше доказательства, не более
rc=2
```

### machine — машинная часть напечатана в чат

```
ОТВЕТ ВОЗВРАЩЁН — нарушений 1 (tools/hooks/answer_check.sh):
  1. [ФОРМА-5] машинная часть напечатана в чат, строка «=== SESSION LOG · 2026-08-09 · arch_test ===» (К-39 §7): её носители — файлы репо и описание коммита

Перепиши по К-39 и К-42: шапка из трёх строк, вывод раньше доказательства, не более
rc=2
```

### rec_below — рекомендация отослана вниз вместо содержания

```
ОТВЕТ ВОЗВРАЩЁН — нарушений 1 (tools/hooks/answer_check.sh):
  1. [ФОРМА-2] строка «Рекомендация:» отсылает вниз вместо содержания: «см. ниже» (К-39 §6, К-25)

Перепиши по К-39 и К-42: шапка из трёх строк, вывод раньше доказательства, не более
rc=2
```

### long — 26 строк без развилки — лимит 25

```
ОТВЕТ ВОЗВРАЩЁН — нарушений 1 (tools/hooks/answer_check.sh):
  1. [ФОРМА-3] текст к Owner после шапки — 26 непустых строк при лимите 25 (К-39 §2, развилок 0): лишнее уходит в файл репо или в следующую сессию

Перепиши по К-39 и К-42: шапка из трёх строк, вывод раньше доказательства, не более
rc=2
```

### fork3 — три развилки в одном ответе при пределе два

```
ОТВЕТ ВОЗВРАЩЁН — нарушений 1 (tools/hooks/answer_check.sh):
  1. [ФОРМА-3] развилок в одном ответе 3 при пределе 2 (К-25): лишние переносятся в следующую сессию

Перепиши по К-39 и К-42: шапка из трёх строк, вывод раньше доказательства, не более
rc=2
```

### tail_conclusion — вывод спрятан в последнюю строку (К-42 §1, §7)

```
ОТВЕТ ВОЗВРАЩЁН — нарушений 1 (tools/hooks/answer_check.sh):
  1. [УПАКОВКА-1] вывод спрятан в последнюю строку: «Поэтому рекомендация — вариант Б.» (К-42 §1, §7) — он должен стоять до доказательства

Перепиши по К-39 и К-42: шапка из трёх строк, вывод раньше доказательства, не более
rc=2
```

### journal — журнал собственных действий вместо результата (К-42 §3)

```
ОТВЕТ ВОЗВРАЩЁН — нарушений 1 (tools/hooks/answer_check.sh):
  1. [УПАКОВКА-2] журнал собственных действий вместо результата: строк-шагов «сначала/затем/потом/далее» — 3 (К-42 §3)

Перепиши по К-39 и К-42: шапка из трёх строк, вывод раньше доказательства, не более
rc=2
```

### telemetry — телеметрия вместо смысла (К-42 §4)

```
ОТВЕТ ВОЗВРАЩЁН — нарушений 1 (tools/hooks/answer_check.sh):
  1. [УПАКОВКА-3] телеметрия вместо смысла, строка «- шагов 36/36, разделов 36 и 14, запрещённых префиксов 0/0, проверок 4/5, дат 0/0» (К-42 §4): число выводится, только если меняет вывод или нужно Owner для решения

Перепиши по К-39 и К-42: шапка из трёх строк, вывод раньше доказательства, не более
rc=2
```

### codes — плотность кодов в одной строке (К-42 §6)

```
ОТВЕТ ВОЗВРАЩЁН — нарушений 1 (tools/hooks/answer_check.sh):
  1. [УПАКОВКА-4] плотность кодов в одной строке (К-42 §6), строка «Гейт ADR-024 пройден, ADR-083 прецедент прямой, ADR-033 не переоткрывается, К-15 не мешает.»: код остаётся, если читатель что-то понимает благодаря ему

Перепиши по К-39 и К-42: шапка из трёх строк, вывод раньше доказательства, не более
rc=2
```

## Судья упаковки (ADR-123 §2) — прогон отказа

Судья написан и самотестируется, но не работает: CLI `claude` требует отдельной
авторизации. Дословный вывод самопроверки:

```
bash tools/hooks/answer_judge.sh --selfcheck
answer_judge: CLI claude не авторизован — судья не работает. Порядок включения в шапке /Users/ilyasbazarov/Downloads/dom-prorab-ecom-arch/tools/hooks/answer_judge.sh
```

Отказ не тихий: строка уходит в замер тегом `СУДЬЯ-ОТКАЗ`, свод её показывает.

## Замер (ADR-123 §4)

Прогоны `--text` в замер не идут — иначе самотест утопил бы картину реальных ответов.
Проверено кейсом самотеста «прогон --text в замер не идёт».

```
selftest: пройдено 73, провалено 0 (локали: C, en_US.UTF-8)
```

Было 60 при 30 кейсах (ADR-122), стало 73 при 36 кейсах в двух локалях плюс один кейс замера.
