# FILE: reference/2026-08-01_search_locale_measure.md

# Замеры: локаль и сплошной поиск К-27; ретро-прогон четырёх аттестаций

Провенанс: сессия arch_open_questions_5, 2026-08-01, HEAD 7b556fa, дерево чистое.
Окружение прогона: git 2.39.5 (Apple Git-154), по умолчанию LANG="" / LC_CTYPE="C".
Файл — снимок замеров, после коммита не правится (К-33).

## 1. Локаль против флага регистра
Дерево 37ba6ed9, вне l3-external, понятие «признак исполнения».

| Команда | Совпадений |
|---|---|
| `LC_ALL=C git grep -n -i "признак исполнения" 37ba6ed9 -- ':!l3-external'` | 4 |
| `LC_ALL=en_US.UTF-8 git grep -n -i "признак исполнения" 37ba6ed9 -- ':!l3-external'` | 31 |
| `LC_ALL=ru_RU.UTF-8 git grep -n -i "признак исполнения" 37ba6ed9 -- ':!l3-external'` | 31 |
| `git grep -n -E "(П\|п)ризнак исполнения" 37ba6ed9 -- ':!l3-external'` | 31 |

Выдачи строк 2 и 4 сверены построчно: `diff` пуст. Вывод: занижение даёт локаль
окружения, не флаг `-i`. Доступные UTF-8 локали на машине: en_US.UTF-8, ru_RU.UTF-8.

## 2. Недостаточность явного класса
Дерево 7b556fa, вне l3-external.

| Понятие | явный класс первой буквы | `-i` под ru_RU.UTF-8 | `-i` под C |
|---|---|---|---|
| стоп-условие | 15 | 15 | 15 |
| проверка | 73 | 78 | 54 |
| ревью | 82 | 102 | 76 |
| шрам | 4 | 27 | 4 |

Причина: класс `(Ш|ш)` не покрывает `ШРАМ`. Прописных кириллических слов от четырёх букв
в репо вне l3-external — 346 вхождений; наиболее частые: ТОЛЬКО 27, ШРАМ 23, РЕВЬЮ 20,
ОДИН 14, СТОП 9, СОДЕРЖИМОЕ 7.

## 3. Ретро-прогон аттестаций К-27
Аттестаций в логе четыре: docs/02_ADR_LOG.md@7b556fa:977 (ADR-043 §4), :1049 (ADR-044 §6),
:1103 (ADR-045 §6), :1208 (ADR-046 §7).

| Запись | Использовался ли git | Итог |
|---|---|---|
| ADR-043 §4 | нет, чатовый путь (сказано в самой записи, :979-983) | локалью не затронута |
| ADR-044 §6 | нет, «git-команд по репо не выполнялось» (sessions/2026-07-31_arch_m1_next_step_review.md:13) | локалью не затронута; дефект охвата — см. §4 |
| ADR-045 §6 | да | переиздана ADR-046 §2; числа подтверждены: 109 файлов, 15 каталогов на 5e69b8af |
| ADR-046 §7 | да, явный класс | сверено с `-i` под UTF-8, расхождений нет, 31 подтверждено |

Проверка чисел ADR-045 §6:
`git ls-tree -r --name-only 5e69b8af | wc -l` → 109;
`git ls-tree -r -d --name-only 5e69b8af | wc -l` → 15. Поправка ADR-046 §1 верна.

## 4. Дефект охвата §6 ADR-044
Дерево 52cd4d58, вне l3-external. Заявленный охват: «все файлы репо с расширением .md и
.sh, кроме l3-external». Команды:
`LC_ALL=ru_RU.UTF-8 git grep -n -E "\[сейчас\]|\[пачкой [AB]|\[задачей|\[отдельным коммитом\]|\[без правок\]" 52cd4d58 -- ':!l3-external'`
`LC_ALL=ru_RU.UTF-8 git grep -n -i "маркер" 52cd4d58 -- ':!l3-external'`

Счёт форм в docs/02_ADR_LOG.md: `[сейчас]` 19, `[пачкой A|B]` 15, `[задачей]` 4,
`[отдельным коммитом]` 1, `[без правок]` 2. Сумма 41 — совпадает с §6 ADR-044.
Поправка ADR-046 §1 (15 вместо 14 для формы `[пачкой X]`) подтверждена.

Файлы в охвате, не названные в блоке последствий ADR-044:
- `reference/portable_method_2026-07-29.md` — «маркер» на :129, :145, :156, :159, :491,
  :989, :992, :1059; формы на :156, :989, :990, :1058. Строка :156 перечисляет четыре
  формы против пяти по ADR-044 §1.
- `sessions/2026-07-30_arch_portable_method_recovery.md` — формы, 15 строк (:77-:82,
  :102-:104, :129, :131, :150-:152, :176).

Проверка цитаты ADR-044: `reference/2026-07-29_portable_method_review.md:127` несёт
«блока последствий без маркера времени» — цитата верна. Строка :126 того же файла несёт
форму `[сейчас]` внутри того же предложения (:125-:129) и отдельного вердикта не требует.

Разница, которую даёт локаль на том же понятии:
`LC_ALL=C git grep -n -i "маркер" 52cd4d58 -- ':!l3-external'` → 15;
`LC_ALL=ru_RU.UTF-8 git grep -n -i "маркер" 52cd4d58 -- ':!l3-external'` → 17.
Теряются `reference/portable_method_2026-07-29.md:156` и `:989` (слово «Маркеры»,
прописная М). Совпадение с пропущенными вердиктами численное, но не причинное:
сессия-автор git не использовала.

## 5. Состояние гейтов
`grep -n -E "LC_ALL|LANG|locale" tools/hooks/pre-commit tools/verify.sh` → ни одного.
`tools/hooks/selftest.sh:5` — «Каждый кейс гоняется в двух локалях: C и первая доступная
UTF-8»; :10-14 выбирают локаль из C.UTF-8, en_US.UTF-8, ru_RU.UTF-8. Единственный флаг
регистра в tools/ — selftest.sh:12, по латинским именам локалей. Класс гомоглифов хука
задан явно: tools/hooks/pre-commit:13.

## 6. Прогоны К-27 записей этой сессии
Дерево 7b556fa: 113 файлов, из них 80 вне l3-external; 15 каталогов.

| Команда | Совпадений |
|---|---|
| `LC_ALL=ru_RU.UTF-8 git grep -n -i "сплошн" 7b556fa -- ':!l3-external'` | 29 |
| `LC_ALL=ru_RU.UTF-8 git grep -n -i "grep" 7b556fa -- ':!l3-external'` | 56 |
| `LC_ALL=ru_RU.UTF-8 git grep -n -i "пусков" 7b556fa -- ':!l3-external'` | 69 |
| `LC_ALL=ru_RU.UTF-8 git grep -n -i "ритуал старта" 7b556fa -- ':!l3-external'` | 8 |
| `LC_ALL=ru_RU.UTF-8 git grep -n -i "мандат" 7b556fa -- ':!l3-external'` | 51 |
