# Перепрогон проверки полноты переноса (ADR-106 §2) по служебным частям редакции 6 F4-16

Дата прогона: 2026-08-08. Сессия `arch_f4_16_rev5` (Architect режим A, Claude Code, Opus).
Основание: решение Owner 2026-08-08 закрыть RED-1 и RED-2 прогона по редакции 5 редакцией 6.
Предыдущий прогон: `reference/2026-08-08_package_check_adr106_rev5.md` — правила 1 и 2 GREEN,
правило 3 RED на двух снимках из пяти.

Предмет: таблицы переноса `reference/2026-08-08_listkg_flow_map_rev6.md` строки 898-917 (служебная
черта 724) и `reference/2026-08-08_bitrix_flow_map_rev6.md` строки 1033-1040 (служебная черта 883).
Объём тот же, что у прошлого прогона: три правила ADR-106 §2. Трассировка тикетов ADR-038 §3 не
прогонялась, скрипт `tools/package_check.sh` не написан, M1-12 остаётся PENDING.

## Что изменилось против прошлого прогона

Две строки таблиц, обе — предмет находок RED.

**RED-1 закрыт.** Строка `listkg_rev6:916` (в редакции 5 — `:916`) читала «состав пяти разделов»;
теперь читает «шесть разделов: пять нумерованных плюс „Что мы просим в ответ"» и называет семь
адресов-носителей строками выше.

**RED-2 закрыт.** Строка `bitrix_rev5:1039` называла один пункт письма от 7 августа; в редакции 6
на её месте две строки — `bitrix_rev6:1039` (п.1, своя курсорная позиция у обработчиков) и
`bitrix_rev6:1040` (п.2, снятие `order_partial`).

## Правило 1 — адрес существует

Новые адреса, появившиеся правкой, и их разрешение (заголовки печатаются со своими номерами
строк, К-28):

| Адрес | Файл и строка заголовка |
|---|---|
| раздел 2.1 | `listkg_rev6:87` |
| раздел 2.2 | `listkg_rev6:111` |
| раздел 2.3 | `listkg_rev6:142` |
| раздел 3.6 | `listkg_rev6:476` |
| раздел 3.7 | `listkg_rev6:514` |
| раздел 3.8 | `listkg_rev6:540` |
| раздел 6 (пункты «Второе», «Третье», «Шестое») | `listkg_rev6:648`, пункты `:660`, `:670`, `:696` |
| раздел 5.2, абзац «Что из-за этого меняется у вас» | `bitrix_rev6:822`, абзац `:831` |

Все прежние адреса прошлого прогона сохранены без изменений и повторно разрешились.

**Итог правила 1.** Адресов названо **32** (25 прежних плюс семь новых у строки редакции 1 снимка
маппинга и один новый у п.1 письма от 7 августа). Не разрешившихся: **0**. Вердикт: **GREEN**.

## Правило 2 — склейка не уменьшает счёт

Строк, называющих больше одного пункта-источника, теперь две:

- `listkg_rev6:902` — два пункта-источника (§3 плюс вопрос о трёх вебхуках), два адреса. 2 ≥ 2;
- `listkg_rev6:916` — шесть пунктов-источников (шесть разделов редакции 1 снимка маппинга), семь
  адресов. 7 ≥ 6.

**Итог правила 2.** Строк-кандидатов 2, нарушений **0**. Вердикт: **GREEN**.

Прежнее замечание NOTE-1 прошлого прогона в силе и правкой не закрывается: строка `listkg_rev6:905`
(закрывающий блок письма A редакции 5) называет один пункт-источник и ноль адресов, потому что
пункт снят решением Owner (ADR-108 §1), а триггер-форма правила 2 такие строки не ловит. Это
дефект формы правила, не документа; будущий скрипт M1-12 обязан различать «снято решением» и
«потеряно».

## Правило 3 — источник покрыт целиком

| Снимок | Пунктов в снимке | Названо строками таблицы | Вердикт |
|---|---|---|---|
| `reference/2026-08-07_listkg_outbound_rev5.md` | 6 | 6 | GREEN |
| `reference/2026-08-07_listkg_status_mapping_rev2.md` | 6 | 6 | GREEN |
| `reference/2026-08-07_listkg_status_mapping.md` (редакция 1) | 6 (`grep -c '^## '` = 6) | **6** | **GREEN**, был RED |
| `reference/2026-08-07_bitrix_followup_criterion.md` | 1 (собственная нумерация письма, строка 3) | 1 | GREEN |
| `reference/2026-08-07_bitrix_corrections_sent.md` | 2 (`grep -n -E '^[0-9]+\. '` выше служебной черты 22: строки 4 и 15) | **2** | **GREEN**, был RED |

**Итог правила 3.** Снимков проверено 5, RED — **0**. Вердикт: **GREEN**.

Замечание NOTE-2 прошлого прогона в силе и правкой не закрывается: правило 3 написано под
markdown-заголовки, а оба снимка-письма нумеруют пункты прозой. Счёт по ним вёлся по собственной
нумерации письма. Будущий скрипт M1-12 обязан считать пункты-источники обеими формами, иначе на
письмах он молча даст 0 и повторит RED-2.

## Вердикт перепрогона

Правило 1 — **GREEN** (0 неразрешившихся из 32 адресов). Правило 2 — **GREEN** (0 нарушений при
двух строках-кандидатах). Правило 3 — **GREEN** (0 RED из пяти снимков).

Обе находки прошлого прогона закрыты. Открытыми остаются два замечания к ФОРМЕ правил (NOTE-1 и
NOTE-2), адресованные будущему скрипту M1-12, а не документам редакции 6.

Текст, уходящий подрядчикам, правкой не затронут: `diff` первых 723 строк документа List.kg и
первых 882 строк документа Битриксу между редакцией 5 и редакцией 6 даёт по 0 строк расхождения.
