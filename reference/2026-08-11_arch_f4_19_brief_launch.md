# FILE: reference/2026-08-11_arch_f4_19_brief_launch.md

# Собранный пуск Architect режим B — выпуск брифа F4-19 (вторая часть)

Собран сессией `arch_method_fix` 2026-08-11 по требованию Owner. Копируется ЦЕЛИКОМ одним
сообщением и не редактируется: слотов на заполнение нет ни одного, SHA роль берёт сама
(ADR-117 §1, `docs/09_RUNBOOK.md` §4.4).

Адреса ниже верны на коммите, которым применена ADR-127. Появились новые коммиты до запуска —
файл пересобирается сессией, а не правится руками.

---

Ты внутри репозитория dom-prorab-ecom-arch, файлы читаешь с диска, curl и SHA не нужны. Первым действием выполни `git rev-parse HEAD`: команда не отработала или хэш не вернулся — стоп, ты запущена не там, доложи Owner и дальше не читай.
Режим этой сессии: Architect режим B (перечень режимов — CLAUDE.md §Мандат).
Блокирует: F4-17 — прогон заказа по шагам карты. Строка 14 очереди стоит ПЕРЕД строкой 5, и пока свагер не разобран, прогон запускать не на чем.
Твой промт — prompts/architect.md, читай его с диска целиком. Раздел «Доступ» в нём — дисциплина чатового аварийного пути и на этой сессии не действует. Законы — CLAUDE.md. SHA сессии = вывод `git rev-parse HEAD`, каждая цитата в формате файл@SHA:строки (К-1, К-21).

ЗАДАЧА. Выпусти бриф `briefs/F4-19.md` по `docs/06_BRIEF_TEMPLATE.md` на ВТОРУЮ часть задачи F4-19. Первая часть (внесение файла в `l3-external/raw/`) исполнена Owner коммитом `fd30f9a` и брифом не покрывается: это класс B. Состав шагов второй части задан ADR-105 §4 и поправлен ADR-127 §3 — не переизобретай его, а доведи до самодостаточного брифа с литеральными адресами, приёмкой числами и return-this.

ПРЕДМЕТ. Предъяви все адреса ДО чтения обязательного контекста (К-36); не нашёлся хотя бы один — стоп и доклад Owner:

* `l3-external/raw/1c_data_contract.yaml:1-864` — сам файл, предмет брифа
* `l3-external/MANIFEST.md:64-76` — абзац провенанса D8, природа файла и ограничение цитирования
* `docs/02_ADR_LOG.md:5607-5684` — ADR-105, §3 и §4 задают расщепление задачи и состав шагов
* `docs/02_ADR_LOG.md:7780-7872` — ADR-127, §3 поправляет постановку, §4 называет незакрытое
* `docs/02_ADR_LOG.md:5511-5606` — ADR-104 §8, форма выхода F4-17, которому F4-19 служит входом
* `docs/05_ROADMAP.md:90` — строка 14 очереди, объём и порядок
* `docs/06_BRIEF_TEMPLATE.md:1-49` — шаблон брифа
* `briefs/F4-05.md:1-87` и `briefs/F4-06.md:1-94` — образцы, названные ADR-105 §4
* `l0-canon/order_flow_e2e.md:1-1383` — шаги карты `OF-01`…`OF-36`, против которых сверяются эндпоинты
* `l2-registers/implementation_gaps.md:1-166` — регистр гэпов, куда ложатся дельты

ЧТО УЖЕ РЕШЕНО И НЕ ПЕРЕСПРАШИВАЕТСЯ (ADR-117 §2 — решение, лежащее в репо, называется адресом):

* Шаги второй части — ADR-105 §4: конвертация в `l3-external/md/` с построчной адресацией и блоком провенанса; перечень эндпоинтов, сопоставленный с шагами карты `OF-NN`; дельты строками в регистр гэпов с адресатом.
* Вердикт «реализовано» из файла НЕ выводится: описание интерфейса не есть прогон (К-1, ADR-105 §4). Это вход F4-17, а не его замена.
* Файл — производный контракт, а не машинный экспорт свагера (ADR-127 §3). Перечень из 13 эндпоинтов НЕ доказанно полон относительно действующего свагера по пути `/api/v1/swagger/`. Вывод «эндпоинта нет» из файла не делается — К-14.

ЧТО НЕ РЕШЕНО И РЕШАЕШЬ ТЫ. Как бриф обходится с неустановленной полнотой файла (ADR-127 §4): ограничением в разделе «Вне scope» и оговоркой в приёмке ЛИБО отдельным шагом discovery, который поднимает вопрос до исполнения. Решение Owner на этот выбор НЕ получено — объяви это в записи прямо (ADR-117 §3), запись выйдет `proposed` по К-40. Рекомендация сборщика: ограничение плюс оговорка — discovery-шаг требует доступа к порталу, которого у Executor нет, и превратит бриф в ожидание.

ТРЕБОВАНИЯ К БРИФУ:

* Поле «Служит / Блокирует» обязательно: без него бриф не выпускается (ADR-052, К-31 к полю «Файлы на запись»).
* «Файлы на запись» — маркированным списком, путь первым токеном строки (К-31): бриф пишет в `l3-external/md/`, `l3-external/MANIFEST.md` и `l2-registers/implementation_gaps.md`, и пересечения с другими задачами очереди быть не должно.
* Приёмка формулируется утвердительно и числом (К-28): не «расхождений не найдено», а «эндпоинтов сопоставлено N из 13, шагов карты без эндпоинта — M, строк дельт заведено K». Пустой вывод результатом не является.
* Блок провенанса конвертированного файла — по образцу D7 и D8: размер, sha256 первые 16, число строк.
* Context-to-load несёт литеральные адреса, а не голые пути (К-18 для чатового пути, полезно и здесь).
* Бриф самодостаточен: Executor не должен догадываться и не должен читать эту сессию.

Перед брифом проверь, нет ли по задаче нерешённых развилок: поверх нерешённого бриф не генерится. ADR-127 `proposed` этому не мешает — она санкционирует уже случившееся и объём брифа задаёт, но если увидишь в ней дефект, это находка, а не повод остановиться.

Конец сессии — один session-блок по `docs/04_SESSION_PROTOCOL.md` в описание локального коммита. `git push` не выполняй.
