# SESSION LOG · 2026-08-09 · arch_f4_20_requirements

**Тип:** Architect режим A (Claude Code, Opus). **SHA сессии:**
`83a4fb172cd5d8cec89a335f6d4f55dc203898e7`. **Дата проверена командой `date`:**
`Sun Aug  9 19:28:17 +06 2026` (К-29).

**Задача.** F4-20 — производная F4-16 (ADR-112 §6), форма выхода задана ADR-113 §3. Четыре
разрыва класса Б (Д-01, Д-02, Д-09, Д-13) редакция 7 документа Битриксу передавала адресату
ВОПРОСОМ там, где по классификации ADR-111 §4 нужно ТРЕБОВАНИЕ. Назвать требуемое поведение по
каждому с наблюдаемым признаком приёмки и выпустить редакцию 8.

**Сделано.**

1. Требуемое поведение названо по всем четырём разрывам, у каждого признак приёмки в форме
   «предусловие → действие → что увидим» (ADR-116 §1-§3).
2. **Д-01 и Д-02 сведены в ОДИН раздел 4.4, а не разложены врозь.** Цепочка принудительная:
   INV-4 требует, чтобы номер существовал всегда → сделка без номера не создаётся → отказ
   генератора превращается в отказ создания сделки → ловится тем же, чем недоступность CRM.
   Механизм — три правила: заказ сначала сохраняется на сайте, передача идёт с повтором по
   нарастающему интервалу и с идемпотентностью по клиентскому номеру, непереданный за 15 минут
   заказ даёт алерт. Четвёртый исход, отвергнутый склад, автоповтора не получает: это неверные
   данные, а не сбой связи. Ручной ввод `UF_CLIENT_ORDER_NO` запрещён строкой таблицы 2.2.
3. **Д-09** — отказ создания заявки сделку не двигает: стадию 8 открывает только полученный
   `claim_id`. Задача менеджеру, три автоповтора по пять минут, дальше человек; второй порог
   сторожа на стадию 7 — один час, потому что заказ уже собран и слот назначен.
4. **Д-13 расщеплён, наружу ушла половина подрядчика** (К-41, ADR-114 §5): шаблон, триггер на
   вход сделки в стадию 2, четыре переменные, SMS-дублёр 60 секунд. Про кабинет провайдера и
   модерацию в документе нет ни строки — проверено числом приёмки (совпадений 0).
5. **ПУШБЕК ПО ВХОДАМ: номер шаблона исправлен `#17` → `#19`.** Черновик
   `reference/2026-08-09_f4_20_inputs.md@83a4fb1:188` предлагал `#17`, Owner принял ТЕКСТ шаблона
   без правок, номер фактурой не проверялся. Реестр прямо говорит «Шаблоны \#12 (tracking) и
   \#17 (waitlist) исключены из данной итерации»
   (`l3-external/md/d4_t07_reestr_wa_hsm_shablonov.md@83a4fb1:76`), `#18` занят напоминанием об
   оплате (`@83a4fb1:74,586`). Первый свободный — `#19`. Текст, переменные и триггер не менялись.
6. Выпущена `reference/2026-08-09_bitrix_flow_map_rev8.md`. Редакция 7 не тронута ни строкой
   (К-33) и адресату не показывалась (ADR-113 §1). Блока дельты против редакции 7 выше служебной
   черты НЕТ (ADR-113 §3).
7. Обязательство ADR-115 закрыто ДВУМЯ ЧИСЛАМИ: подвопросов записи CQ-35 в шаге `OF-01` — 5 из 5;
   строк раздела 7 без формулировки по названному адресу — 0.
8. Записи CQ-31 и CQ-32 СНЯТЫ: вопрос заменён требованием — исход, под который они объявлены
   временными. Ключи `Q-14`…`Q-16` сохранены, но спрашивают теперь о РАСХОЖДЕНИИ с названным
   требованием. Счёт вопросов раздела 7 пересчитан и равен 10 — столько же, сколько в редакции 7,
   при полностью изменённом содержании четырёх строк.
9. Перечень порядко-зависимых утверждений ПЕРЕСЧИТАН СПЛОШНЫМ ПРОГОНОМ (ADR-113 §4, правило
   ADR-109 §1): 26 совпадений формы поиска, порядко-зависимых ВОСЕМЬ, девятого нет. Связка
   перенесена на пару `listkg_rev7` плюс `bitrix_rev8`.

**Числа приёмки редакции 8.** Служебная черта 1058 → 1229, текст наружу 1056 → 1227. `diff`
текста наружу: 9 ханков, удалена 21 строка, добавлено 192; разложение по двум заявленным классам
(требования F4-20; дата документа) без остатка, ханков вне классов 0. Строк шага в разделе 1 — 36,
разделов шага в 2.9 — 36, стадий 14, значений статуса 1С 5, строк 2.7 — 14, строк 2.8 — 9.
Статусный контур: 6 и 8 при сумме 14, значений статуса без стадии 0, пустых клеток 0 —
подразделы 2.4-2.8 в `diff` не участвуют. Подразделов раздела 4 — 6, требований без признака
приёмки 0, требований 4.4-4.6 без вопроса о расхождении 0. Внутренних кодов выше черты 0,
упоминаний нашей половины Д-13 выше черты 0, слой переписки 21 совпадение (15 ссылок на выданное
задание плюс 6 ложных), ссылок на наши письма 0, обещаний будущей присылки 0. Строк `Q-NN` — 10,
вопросов без формулировки по адресу 0, вопросов в тексте вне раздела 7 — 0.

**Найдено попутно.**

- **Расхождение имени парного файла.** Служебная часть
  `reference/2026-08-08_listkg_flow_map_rev7.md@83a4fb1:1182` называет парным документом редакцию
  7 документа Битриксу, а уходит редакция 8. Снимок править нельзя (К-33), ADR-113 §4 отдаёт
  правку «редакции, которая выйдет последней» — последней вышла редакция 8, и чужую служебную
  часть она исправить не может. Ниже черты, адресату не видно, чисел связки не меняет. Развилка
  подана Owner (ADR-116 §7).
- **Пусковое сообщение пришло с незаполненным блоком решений Owner** — четыре строки остались
  шаблоном `<принят / принят с правкой / отклонён>`. Два решения восстановлены из репо (ADR-114:
  концепт Д-02 и текст шаблона приняты Owner дословно), два приняты РОЛЬЮ — концепт Д-01 и
  правило Д-09. Отсюда статус записи `proposed` (К-40).
- **Сторожевой хэш пускового сообщения устарел.** Тело сообщения велело остановиться при хэше,
  отличном от `9362a96`, тогда как первая строка того же сообщения называет `83a4fb1` — и
  содержание задачи (CQ-35, ADR-115, решения Owner по Д-02 и шаблону) существует только на
  `83a4fb1`. Работа шла на `83a4fb1`, расхождение доложено Owner.

**Не сделано намеренно.** Ответа подрядчика задача не ждёт ни в одной части. Отправка комплекта —
класс B, мандат сессии её не покрывает; `git push` не выполнялся. Редакция 7 обоих документов,
канон, `reference/2026-08-09_f4_20_inputs.md` не тронуты ни строкой.

**Гейты до отправки.** Заход Reviewer на дельту редакции 6 → 8 (ADR-113 §2, полный) и снятие
стопа решением Owner (ADR-095 §1).

---

## Блок 2 · 2026-08-09 · развилка Р-9 закрыта, выпущена редакция 9 документа List.kg

**SHA на входе блока:** `0193cc5173498796ee9d61e626bf6cce6c1a7bd1`. **Дата проверена командой
`date`:** `Sun Aug  9 21:01:36 +06 2026`.

**Что случилось между блоками.** Пока готовился блок 1 и первая попытка закрыть развилку Р-9, в том
же рабочем дереве работала сессия `arch_f4_16_owner_gate` и сделала четыре коммита — 930b858,
f7f12b5, 4993036, 0193cc5. Нарушено CLAUDE.md §Параллельные сессии. Owner подтвердил, что вторая
сессия в дерево больше не пишет.

**Решение Owner по развилке Р-10** — вариант А: новая редакция, редакцию 8 не править и не удалять.
Вместе с ним три поправки к диагнозу роли, все приняты:
1. Редакция 8 — НЕ черновик и НЕ сирота: она выпущена этой сессией по решению Owner о развилке Р-9
   и несёт его в служебной части. Формулировка роли «в репо закоммичен файл, который никто не
   выпускал» была неверна и снята.
2. Закоммитила её не роль, а параллельная сессия — командой `git add -A`, подмётшей чужой
   незакоммиченный файл под своё сообщение. Дефект признан за той сессией.
3. ТРЕТЬЯ ПОЛОМКА, роль её не увидела: КОЛЛИЗИЯ НОМЕРА РЕШЕНИЯ. Редакция 8 ссылалась на `ADR-117`
   как на решение по Р-9, а этот номер занят записью параллельной сессии о форме пускового
   сообщения; записи по Р-9 в журнале не было ни одной. Ссылка вела не туда — дефект тяжелее имени
   парного файла, потому что рвёт связь документа с решением, которым тот выпущен.

**Сделано.**

1. Выпущена `reference/2026-08-09_listkg_flow_map_rev9.md`. Исправлены обе сломанные ссылки: имя
   парного файла — `reference/2026-08-09_bitrix_flow_map_rev9.md`, основание — ADR-120.
2. Номер решения взят ПЕРЕЧИТЫВАНИЕМ `docs/02_ADR_LOG.md` непосредственно перед записью: последняя
   запись ADR-119, первый свободный — ADR-120.
3. Служебная часть приведена к факту: редакция 8 была закоммичена в середине правки, и её блок
   приёмки остался описывающим редакцию 7 при заголовке «Редакция 8».
4. Редакция 8 не тронута и не удалена (К-33), в записи названа прямо: выпущенный снимок, провенанс
   решения по Р-9, отправке не подлежит.
5. Оба дефекта параллельного контура заведены строками `docs/00_STATE.md` §Открытые вопросы (К-23)
   с правилами-кандидатами: коммит только явными путями, никогда `-A`, когда дерево может нести
   чужую работу; номер решения берётся перечитыванием журнала непосредственно перед записью.
6. Предмет захода Reviewer сдвинут на дельту 6 → 9 ОБОИХ документов одним заходом (ADR-120 §5).

**Числа приёмки редакции 9.** Служебная черта 853, не сдвигалась. Текст наружу — строки 1..851.
`diff` текста наружу редакции 8 и редакции 9 — **0 строк расхождения**: обе правки ниже черты.
`diff` против редакции 7 — 1 ханк в одной строке, это строка даты; ханков вне заявленного класса 0.
Счётчики совпали с редакцией 7 до единицы: шагов в разделе 1 — 36, разделов шага 2.7 — 14, значений
статуса 5, строк 2.4 — 14, строк 2.5 — 14, строк 2.6 — 9, статусный контур 6 и 8 при сумме 14,
внутренних кодов выше черты 0, слой переписки 19, строк «Проверка:» 5, строк `Q-NN` 12. Вхождений
`ADR-117` и `bitrix_rev8` в файле — по одному, оба в блоке «Что изменено против редакции 8» и
описывают исправляемое, а не действующее.

**Не сделано намеренно.** Отправка — класс B, мандатом не покрыта; `git push` не выполнялся.
Редакции 7 и 8 документа List.kg, редакции 8 и 9 документа Битриксу не тронуты ни строкой.
