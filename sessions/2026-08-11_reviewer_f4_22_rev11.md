# SESSION · 2026-08-11 · Reviewer (Claude Code, Fable) · reviewer_f4_22_rev11

**SHA сессии:** `fda0d14e5be31367fdba34bde9fc9786c567cc05`
**Проверка признака пути (дословный вывод):** `git status --porcelain` — пусто;
`git rev-parse HEAD` → `fda0d14e5be31367fdba34bde9fc9786c567cc05`. Самотест хука:
«selftest: пройдено 88, провалено 0 (локали: C, en_US.UTF-8)».
**Предмет (адреса предъявлены до чтения обязательного контекста, К-36):**
`reference/2026-08-11_bitrix_flow_map_rev11.md` целиком — 2106 строк по `wc -l`, служебная
черта на строке 1429, текст наружу 1..1428; `l2-registers/contractor_questions.md:42-50` —
строки `CQ-36`…`CQ-44`. Оба адреса найдены на SHA сессии (`git ls-tree fda0d14` — оба блоба
на месте).
**Точка ревью:** перед отправкой пакета подрядчику (ADR-024). Артефакт выпущен задачей
`F4-22` (бриф `briefs/F4-22.md`, ADR-132) коммитом `fda0d14`. После захода остаются: апрув
ADR-132 (Owner), решение об отправке пары «редакция 11 + `listkg_rev9`» — класс B.

## 1. Свой прогон машинных проверок брифа (шаги 7, 8, 9)

- **Дельта против редакции 10 (шаг 9):** `diff` текста наружу 1..1237 (редакция 10, черта
  1238) против 1..1428 (редакция 11, черта 1429) — **193** строки: удалена **1** (счётная
  строка раздела 7 «Вопросов десять…» → «Вопросов девятнадцать…»), добавлено **192**. Ханки:
  `27a28` (строка навигации раздела 0), `1227a1229,1237` (девять строк `Q-23`…`Q-31` таблицы
  раздела 7), `1229c1239` (счётная строка), `1230a1241,1421` (весь раздел 8). Строк вне трёх
  разрешённых мест — **0**, удалённых сверх счётной — **0**. Числа Return-this воспроизведены.
- **Внутренние коды выше черты (шаг 8):** дословная команда брифа по 1..1428 — совпадений
  **0**. `Д9-15`/`getOrderStatus` выше черты — **0** упоминаний.
- **К-41 (шаг 7):** дословная команда по 1..1428 — **21** совпадение, все на строках ≤1224,
  то есть в тексте, унаследованном от редакции 10 (там тоже 21); раздел 8 множество не
  расширяет, новых утверждений о нашей переписке **0**.
- **Ключи:** `Q-13`…`Q-22` в диффе не тронуты (только добавления); «Вопросов девятнадцать» =
  19 строкам таблицы (10 прежних + 9 новых); `Q-23`…`Q-31` без пропусков.

## 2. Сверка содержания раздела 8 с нормой и регистром

Все 15 строк дельт разложены по 8.1 / 8.2 / 8.3 в точном соответствии классам и шагам
секции §Дельты D9 регистра (`implementation_gaps.md:151-166`): `Д9-16`→8.1, `Д9-01`…`Д9-05`→8.2
п.1-5, `Д9-06`…`Д9-14`→8.3 п.1-9. Требуемое поведение каждой строки сверено с полями
«Направление» / «Вызов»/«Событие» / «Ответ» соответствующего шага `order_flow_e2e.md` §1 —
расхождение одно (находка №1). Требование 8.1 совпадает с ADR-131 §3 и каноном
(`canonical_order_flow.md:121,151`; карта `:471-474`, кнопка-дублёр `:494-495`). Цитаты
спецификации проверены по `l3-external/md/d9_t01_bitrix_api_spec.md`: `:311` (триггер
`FINAL_INVOICE`, Basic Auth), `:372-383` (единственный ответ `200`), `:392`/`:456`
(единственная связанная стадия, HTTP 409), `:2506-2545` (`oneCStatus{code,name}`) — раздел 8
спецификацию не искажает. Шаг 6 брифа подтверждён построчно: `listkg_rev9:288-297` несёт
отдельный канал `CRM_Webhook_URL_OOS` с телом по карте (`OF-12`), `:349-353` — отдельный адрес
`CRM_Webhook_Path_CheckPrinted` (`OF-25`); непокрытых требований по `Д9-06`/`Д9-09` — 0,
связка ADR-103 §3 не переоткрывается; порядко-зависимых утверждений раздел 8 не добавляет
(греп по новому тексту — 0 упоминаний документа List.kg). `CQ-36`…`CQ-44` — 9 строк формата
реестра, адресат `Bitrix-подрядчик`, статус DRAFT, связь `Д9-NN`, ключи наружу и носители
совпадают с разделом 7 и подразделом 8.3; прежние строки реестра не тронуты (в диффе коммита
только добавления). INV-3/INV-4 — находок нет: клиентских точек раздел 8 не касается,
идентификаторы в нём — межсистемные, уместные в документе подрядчику.

## 3. Таблица находок

| № | Уровень | Что не так | Цитата | Чем грозит | Кому |
|---|---|---|---|---|---|
| 1 | FIX | Подраздел 8.2 пункт 1 (`OF-07`) называет «ответ несёт пять исходов» и тут же перечисляет ШЕСТЬ (успех, нехватка, отказ, идемпотентный повтор, ошибка склада, ошибка проведения); «пять» повторено в просьбе («привести … к полному составу из пяти исходов») и в признаке приёмки («несёт все пять исходов»). Карта даёт шесть исходов | `reference/2026-08-11_bitrix_flow_map_rev11.md@fda0d14:1296-1305` против `l0-canon/order_flow_e2e.md@fda0d14:185-195` | Признак приёмки удовлетворяется пятью исходами — подрядчик обновит спецификацию без одной ветки, формально пройдя приёмку; недостача вскроется прогоном `F4-17` переделкой. Внутреннее противоречие «пять — а перечислено шесть» видно адресату и возвращается вопросом | Architect |
| 2 | FIX | Дата шапки — «9 августа 2026 года» — при 192 новых строках текста наружу. Довод, которым дата удержана в редакции 10 («текста наружу не касается ни одной строкой», провенанс), к редакции 11 не применим; решения о дате нет ни в ADR-132 §1, ни в брифе — дыра, а не выбор | `reference/2026-08-11_bitrix_flow_map_rev11.md@fda0d14:3` против `:1576-1580` и 192 добавленных строк наружу (диф `27a28`, `1227a1229,1237`, `1229c1239`, `1230a1241,1421`) | Адресат получает второй документ с той же датой, что уже прочитанный 2026-08-10, и не может отличить редакции; вопрос «что изменилось и когда» возвращается письмом, а раздел 8 описывает сверку со спецификацией, внесённой 2026-08-11 — позже даты документа | Architect (решение), Owner при отправке |
| 3 | NOTE | Служебная часть называет текст наружу редакции 10 «1236 строк»; фактически выше черты редакции 10 — 1237 строк (черта на 1238). Арифметика самого блока сходится только при 1237: 1428 = 1237 + 192 − 1 | `reference/2026-08-11_bitrix_flow_map_rev11.md@fda0d14:1450-1451` против `reference/2026-08-10_bitrix_flow_map_rev10.md@fda0d14:1238` (строка черты) | Ниже черты, наружу не уходит; следующая редакция, взяв константу на веру, унаследует сдвиг на строку — класс ADR-109 §1 | Architect |
| 4 | NOTE | Отправка редакции 11 опирается на решения ADR-132 §1, §3, §5 в статусе `proposed` — по К-15 без апрува они не применяются к необратимому шагу. Тот же апрув закрывает словесное расхождение с ADR-131 §5 («входят также в следующую редакцию документа List.kg») через замер §3: покрытие уже в отправленной `listkg_rev9`, новая редакция не нужна | `docs/02_ADR_INDEX.md@fda0d14:61` (ADR-132 `proposed`); `docs/02_ADR_LOG.md@fda0d14:8166-8173` (ADR-131 §5) против `rev11:1483-1490` (замер шага 6) | Отправка до апрува — необратимая операция на неапрувленном основании; риск процедурный, содержание замером подтверждено. Апрув уже стоит строкой «На Owner лично» | Owner |

## 4. Вывод

Отправлять можно: STOP 0. Находки №1 и №2 — текст наружу, и до отправки цена их правки
нулевая (тот же довод, что у ADR-111 §3); рекомендация — закрыть обе редакцией 12 до
отправки, решение за Architect и Owner. №4 закрывается апрувом ADR-132, уже стоящим на Owner.

## Замечено рядом, вне предмета ревью

- Карта `order_flow_e2e.md:715-717` называет `receipt_type` «обязательным пятым полем» шага
  `OF-28`, хотя тело шага несёт шесть полей до него (формула скопирована с `OF-25`, где полей
  четыре). На выход F4-22 не влияет: редакция 11 счёт полей `OF-28` не воспроизводит.
- Триггер `OF-30` в карте — «72 часа с момента постановки резерва» (`:763`), а таймер по
  `OF-24` стартует кнопкой «Сборка завершена» (`:624-625`); редакция 11 воспроизводит смысл
  («72 часа без события о выдаче»), расхождение живёт внутри карты.
