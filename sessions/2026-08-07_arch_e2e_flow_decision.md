# SESSION LOG · 2026-08-07 · arch_e2e_flow_decision (Architect режим A+B, Claude Code, Opus)

## Задача

Два предмета. Первый: отработать семь находок Reviewer-сессии `reviewer_f4_11_presend`
(коммит `3c53956`) по выходу задачи F4-11. Второй: решить, каким должен быть артефакт,
показывающий ордер-флоу целиком сквозь Bitrix CRM и 1С — с триггерами, ответами, последствиями и
мандатами, — и как по нему становится видно, что подрядчики уже сделали, чего не сделали и что
сделали не так. Плюс: назвать вопросы и гейты списком задач с предусловиями и путями.

## Признак пути и SHA

`git rev-parse HEAD` → `3c539565bac682bc974552e8a4491b10d213b956`, дерево чистое. Путь — Claude
Code (К-21). Хук исполняемый, самотест: пройдено 46, провалено 0 (локали C, en_US.UTF-8). К-20
сошлась на всех прочитанных `docs/NN_*.md` и на `CLAUDE.md`.

## Что прогнано (сверх чтения)

1. Находка №1, послеправочный прогон:
   `LC_ALL=ru_RU.UTF-8 git grep -n -i "два источника" 3c53956 -- 'l0-canon'` — 1 совпадение,
   `l0-canon/canonical_order_flow.md:209`, утверждает единственность источника. Строк канона,
   утверждающих обратное: 0. Контрольный счёт `order_completed` в каноне — 13 строк.
2. Находка №2: подняты `l3-external/md/d6_t05_poslednyaya_milya_last_mile.md@3c53956:85-125`.
   Контракт Эпика 24 — `:87-89`, AC 24.1 и 24.2 — `:95-96`, AC 25.1…25.6 — `:118-123`. Адрес
   `:116-124` из ADR-092 §2 накрывает только Эпик 25. Утверждение §2 по существу подтверждено.
3. Находка №3: `git show c35e205:l0-canon/canonical_order_flow.md | wc -l` → 123 при адресе
   `:225-226,229-230`; на `3c53956` искомые строки — `:219-220`.
4. Находка №5 расширена собственной находкой: `tasks/trackB_wave1_1c.md@3c53956:181-182` требует
   не только неизменной области действия, но и неизменного состава тела события, а состав тела
   меняют две наши более поздние просьбы — `doc_version` (задача `1C-B1-05`, тот же файл
   `:71,74-75,93`) и `receipt_type` (письмо C, раздел 5). Отменены обе строки.
5. Находка №6: `LC_ALL=ru_RU.UTF-8 git grep -n -i "чат (Reviewer)" 3c53956 -- ':!l3-external' ':!sessions'`
   — 2 совпадения. `docs/05_ROADMAP.md:71` — правится (живое предписание), `:58` — остаётся
   (историческая запись о проведённой F4-08).
6. Находка №7: счёт хвоста `proposed` сверен с журналом — семнадцать на `3c53956`, девятнадцать
   после настоящей сессии.
7. Счёт написаний стадии сделки в ТЗ Битрикса: форма `Сделка → «…»` по `l3-external/md/d4_*.md`
   даёт пятнадцать различных написаний при десяти строках канона §3.1; шесть за пределами
   канона, адреса — в ADR-094 §Контекст. Одна форма поиска объявлена нижней границей, полный
   счёт вынесен в шаг 4 брифа F4-14.
8. Спина будущей карты пересчитана: в D1 тридцать пять идентификаторов IC
   (`grep -ohE '^#{2,4} IC-[A-Z]{2}-[0-9]{2}[a-z]?' d1_*.md | sort -u | wc -l` → 35), в scope
   карты двадцать шесть.
9. Письмо C редакции 2 проверено грепом на внутренние коды проекта и пути репо по отправляемой
   части (до служебного блока): совпадений 0.

## Сделано

- ADR-093 (proposed) — шесть находок закрыты по существу, седьмая (статус ADR-091) получила
  явную оговорку и ушла в F4-13. Правило «аттестация переопределяющего решения предъявляется
  прогоном ПОСЛЕ правки» записано §2 и конвенцией пока не оформлено (порядок ADR-087 §2).
- ADR-094 (proposed) — сквозной ордер-флоу оформлен тремя слоями (норма L0, документы наружу,
  регистр реализации), введён стабильный идентификатор шага `OF-NN`, закрыт состав строки карты
  из девяти полей, введена матрица мандатов, исправлен порядок работ Owner тремя пунктами, F4-12
  поглощена F4-16, заведён OQ-08.
- `briefs/F4-14.md` — бриф сквозной карты, режим B.
- `reference/2026-08-07_listkg_status_mapping_rev2.md` — редакция 2 письма C: две вставки,
  отмена двух критериев приёмки тикета `1C-B1-09`.
- `briefs/F4-11.md` — две ссылки на канон переведены на `@3c53956:219-220`.
- `docs/05_ROADMAP.md` — строка F4-13 (счёт и путь), F4-12 помечена поглощённой, добавлены
  F4-14…F4-17, добавлен абзац текущего фокуса.
- Регистры: OQ-08 (тестовый контур и описание REST-интерфейса), CQ-25 и CQ-26 (DRAFT, «что уже
  реализовано» по шагам карты каждому подрядчику).
- `docs/00_STATE.md` — шапка, стенд-ап, развилка Р-1 отдельным блоком (К-25), append подробностей.

## Отклонения

- Пусковое сообщение не несло отдельной строки с именем режима сессии (ADR-049 §3). Стоп по
  ADR-049 §4 наступает при режиме ВНЕ закрытого перечня; здесь строки нет вовсе, а оба режима,
  к которым сводится задача — Architect A и Architect B, — в перечне `CLAUDE.md` §Мандат стоят.
  Работа продолжена, отклонение зафиксировано здесь и строкой журнала. Правило-останов в форме
  «строки режима нет» ни одной записью не задано; заводить его — предмет отдельного решения.
- Пусковое сообщение воспроизводит чатовую формулу «весь выход сессии — одна вставка,
  FILE_PATCHES внутри блока». Путь фактически Claude Code, и для него
  `docs/04_SESSION_PROTOCOL.md:18` предписывает прямые правки файлов плюс локальный коммит с
  session-блоком в описании; прецедент — три блока сессии `arch_f4_11_brief`. Выполнено по
  протоколу пути, не по формуле пускового.
- Развилка Р-1 на момент коммита не закрыта: обе редакции письма C лежат в репо, выбор и
  отправка — Owner, класс B.
- `git push` не выполнялся.
- Модель сессии — Claude Opus 5, биндинг `docs/03_ROLES.md:31` соблюдён.
