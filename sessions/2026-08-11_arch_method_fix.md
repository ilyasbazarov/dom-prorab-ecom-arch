# SESSION · 2026-08-11 · arch_method_fix · Architect режим A (Claude Code, Opus)

**SHA сессии (дословный вывод `git rev-parse HEAD`):**

```
08e4e56647843b9942b0e45760fdc95a7edec350
```

**Блокирует:** скорость закрытия развилок Owner по Фазе 4. Хук `Stop` возвращал законные
ответы ролей — два полевых случая за сутки и третий в этой сессии, — а ADR-123 не переводился
в `accepted`, пока его признаки бьют по принятым К-36 и К-28.

**Мандат:** Architect режим A (перечень — CLAUDE.md §Мандат). Класс A: необратимых операций
сессия не выполняла, `git push` не делался.

## Отчёт К-36: предъявленные адреса

Все 13 адресов пускового сообщения предъявлены ДО чтения обязательного контекста; каждый файл
существует, каждый диапазон лежит внутри файла (счёт строк `wc -l`, дословный вывод в
транскрипте). Перечень: `sessions/2026-08-11_reviewer_method_gate.md:1-142` ·
`docs/02_ADR_LOG.md:7355-7452` и `:7315-7326` · `tools/hooks/answer_check.sh:100-110` ·
`tools/hooks/selftest.sh:257-295` · `tools/hooks/answer_extract.py:21-22` ·
`docs/01_CONVENTIONS.md:216-224` и `:117-155` · `docs/04_SESSION_PROTOCOL.md:30` ·
`prompts/architect.md:14-24,69-70` · `prompts/business.md:10,67-68` ·
`prompts/executor.md:10,54-55` · `prompts/applier.md:8,62-63` ·
`prompts/reviewer.md:18-24,97-113` · `docs/09_RUNBOOK.md:80-81,87-88,108-109,118-119,128-129,248,265`.

## Задача

Корректирующая запись класса PROCESS по находкам захода `reviewer_method_gate`: закрыть
STOP №1 и FIX №2-№6, вынести вердикт по NOTE №8-№11. Выход — ADR-125 `proposed`.

## Сделано

ADR-125 выпущена и применена одним коммитом; развёрнутое содержание — в самой записи
(`docs/02_ADR_LOG.md`, ADR-125), пересказывать его здесь смысла нет.

Ключевое, чего в записи нет: **решение по способу закрытия STOP принято ролью, а не Owner.**
Развилка стояла между точечными исключениями для строки отчёта К-36 и строк приёмки К-28 и
переводом обоих признаков в предупредительный слой. Выбран второй вариант: исключения
закрывают по одному классу за раз, а перечислить все законные конструкции нельзя — К-36, К-28
и К-40 не последние правила, требующие плотных чисел или кодов, и следующий класс обнаружился
бы следующим полевым возвратом. Рекомендация захода совпадала.

## Отклонения

1. **Отклонение от К-15 названо явно** (ADR-125 §7, образец формы — ADR-072 §7). С 2026-08-10
   по настоящий коммит машинерия проверки ответа работала в поле при статусе `proposed` у
   ADR-122, ADR-123 и конвенций К-39, К-42. ADR-122 §8 обосновал срок, но самого отклонения не
   назвал. Цена предъявлена этой же сессией: три законных ответа возвращены, и обнаружено это
   только полем. Норма на будущее не смягчена.
2. **Решение проекта принято ролью** — см. выше. Основание: ADR-117 §3, второй законный ход;
   запись выходит `proposed` по К-40, апрув Owner меняет только строку статуса.
3. **Хук `Stop` работал в старой редакции всю сессию** — до коммита. Это третий полевой случай
   возврата законного ответа, и он зафиксирован дословно: строка отчёта К-36 с адресами предмета
   этой сессии прогнана на обеих редакциях подряд, вывод в
   `reference/2026-08-11_answer_check_run_rev3.md`.

## Признак исполнения (К-34, обе половины)

Негативный прогон: пять классов нарушения формы возвращаются с кодом 2. Позитивный: три
законных ответа, возвращённых проверкой в поле, проходят с кодом 0. Второй заход: ответ не
возвращается вторым разом, исход уходит в замер строкой `ПОВТОР-ВОЗВРАТ`. Носитель —
`reference/2026-08-11_answer_check_run_rev3.md` (К-22). Самотест: пройдено 80, провалено 0
(было 73). `bash tools/verify.sh`: GREEN.

## Чего сессия НЕ делала

Верно на момент первого блока; три из четырёх пунктов сняты блоком 2 ниже — Owner ответил в
той же сессии.

- Перевод ADR-123 в `accepted` — решение Owner (ADR-033 §2), как и вопрос о точечном повторном
  заходе Reviewer.
- Включение судьи упаковки — требует авторизации CLI, учётные данные роль не вводит. Критерий
  его приёмки задан (ADR-125 §8), сам судья не тронут.
- Снятие префикса `[proposed]` у К-39 и К-42 — держится до перевода ADR-123.
- `git push` — не выполнялся.

## Блок 2 · Апрув Owner применён (2026-08-11)

**Решение Owner, дословно:** «вариант А, апрувь ADR-125 и переводи ADR-123 в accepted».
Ответ на развилку Р-1 первого блока.

**Что изменено — только строки статуса и один префикс (К-15, ADR-016):**
- `docs/02_ADR_LOG.md` — статус ADR-123 и ADR-125, по две строки на запись (заголовочная и
  замыкающая); тела записей не тронуты.
- `docs/02_ADR_INDEX.md` — колонка статуса тех же двух строк (К-19).
- `docs/01_CONVENTIONS.md` — префикс `[proposed]` снят у К-39 и К-42. Держался он ровно до
  перевода ADR-123: их действующая редакция включает его пункты. Заодно названы все источники
  действующей редакции К-39 и переиздание хвоста К-42 записью ADR-125.
- `docs/00_STATE.md` — шапка v1.10 → v1.11.

**Отклонения блока.**
1. Повторный заход Reviewer по ADR-125 не назначался, хотя ADR-033 §2 ставит его третьей точкой
   перед переводом PROCESS-записи в `accepted`. Основание — прямое решение Owner тем же ответом
   (вариант А развилки Р-1): запись новых правил метода не вводит, а снимает найденное предыдущим
   заходом, и обе ветки её признака прогнаны носителем
   `reference/2026-08-11_answer_check_run_rev3.md`.
2. Перестановку строк статуса выполнила сама выпустившая запись сессия. Операция механическая,
   творчества нет; прецеденты — ADR-075/ADR-078 и блок 2 захода `reviewer_method_gate`.

**Состояние очереди метода.** Все шесть записей — ADR-074, ADR-085, ADR-122, ADR-123, ADR-124,
ADR-125 — в `accepted`. Остаток один: включение судьи упаковки, оно за Owner (логин CLI), и его
приёмка — отдельный негативный прогон с носителем.

`bash tools/hooks/selftest.sh` → пройдено 80, провалено 0. `bash tools/verify.sh` → GREEN.
`git commit --no-verify` не применялся, `git push` не выполнялся.

## Блок 3 · Судья упаковки не включается (2026-08-11)

**Решение Owner, дословно:** «вариант А, снимай выключатель». Ответ на развилку о судье
упаковки: не включать · включить выборкой · включить на каждый ответ.

**Сделано:** файл-выключатель `.claude/answer_judge.on` снят, судья молчит (проверено прогоном:
без выключателя скрипт выходит нулём до всякой работы). Хук `Stop` в `.claude/settings.json`
оставлен намеренно — без выключателя он стоит один вызов bash, а включение позже остаётся
правкой в одну строку. Решение внесено строкой открытого вопроса `docs/00_STATE.md` и шапкой
STATE; новой записи журнала решений не заводится: правил метода решение не меняет, а
включение судьи и было отдано Owner ещё ADR-123 §2.

**Повод отказа — расход, и он назван честно.** Судья есть отдельный запуск `claude -p` на
КАЖДЫЙ ответ роли; платится не рубрика, а системный контекст каждого запуска. ЦИФРЫ РАСХОДА
НЕТ и она не оценивалась: замерить можно только при авторизованном CLI, а логин Owner не делал.
Утверждать «дорого» числом сессия не может и не утверждает — решение принято по механике, а не
по замеру, и это записано прямо.

**Что осталось без машинного исполнителя.** Тонкая часть К-42, требующая суждения: её держат
дисциплина роли и возврат ответа Reviewer либо Owner. Четыре грубых класса работают как есть —
два возвращают ответ, два предупреждают (ADR-125 §1).

**Чем строка закрывается.** Либо замером расхода и решением по числу, либо вариантом Б той же
развилки — судья по выборке (каждый N-й ответ): замер за десяток сессий тот же, расход кратно
меньше. Вариант Б есть правка судьи и требует отдельной записи. Критерий приёмки судьи на
случай включения задан ADR-125 §8 и не меняется.

**Состояние очереди метода: закрыта полностью, остатка нет.** Работа возвращается на
продуктовый критический путь ADR-095 §5, ближайший шаг — F4-16.

## Блок 4 · Стенд-ап приведён к функции, причина роста закрыта носителем (2026-08-11)

**Требование Owner, дословно:** «это не стендап это какая-то поэма полотно текста которое я
никогда читать не буду это не функциональная история… мне нужно чтобы вкратце и четко было
написано что сделали что должны делать след шагами, если есть варианты то какие + твоя
рекомендация». Плюс упрёк по существу: правила о форме текста вводились в этой же сессии.

**Что было.** Раздел «Стенд-ап» — 659 непустых строк и 8480 слов; строка шапки `**Версия:**` —
20693 символа. Протокол при этом предписывал пять полей ОДНОЙ строкой. Расхождение с собственной
предписанной формой на два порядка.

**Почему так вышло — причина установлена, не предположена.** У стенд-апа не было носителя
проверки ни одного дня: К-39 и К-42 регулируют текст к Owner в ЧАТЕ, а стенд-ап есть файл репо,
и ни pre-commit, ни `verify.sh` на него не смотрели. Это ровно диагноз ADR-122 §0.1, применённый
к другому артефакту. Второй слой — append дешевле правки: вырезать чужой абзац страшнее, чем
дописать свой, отсюда наслоение «Раньше значилось… Прежняя формулировка…».

**Собственный вклад назван.** Три последних блока в строку `**Версия:**` дописала эта же сессия,
в том же заходе, где писала правила о форме. Упрёк Owner принят как фактура, а не как тон.

**Сделано.** Стенд-ап переписан пятью полями, 14 непустых строк вместо 659. Прежний раздел и
прежняя строка шапки перенесены в `docs/00_STATE_ARCHIVE.md` ДОСЛОВНО (К-19) — содержание не
выброшено. Форма закреплена в `docs/04_SESSION_PROTOCOL.md` §STATE_PATCH одним экземпляром.
Лимит сделан машинным: проверка 6b `tools/hooks/pre-commit` — 20 непустых строк раздела и 900
символов строки шапки; три кейса самотеста в двух локалях, 86 против 80. Выпущена ADR-126.

**Факт по F4-16 внесён.** Задача закрыта (подтверждено Owner), строка 2 очереди роудмапа
приведена к факту, следующий шаг назван адресом: строка 4 — снятие стопа и отправка пары,
класс B, действие Owner.

**Два дефекта поймано самотестом до коммита, оба записаны в ADR-126 §5.1.** Первая редакция
проверки 6b читала файл пайпом в `grep -m1` и умирала по SIGPIPE с кодом 141 на настоящем
300-килобайтном STATE: коммит не проходил, причина не называлась. Самотест это зеленил, потому
что ветка `fail` принимала любой ненулевой код, а фикстура была короткой и в буфер влезала.
Вторая: лимит строки шапки считался в `${#строка}` и под UTF-8 мерил символы, под локалью C —
байты, то есть один порог работал по-разному (К-35). Обе починены, единица названа байтами,
`run_case` теперь бракует смерть хука по сигналу всегда.

**Отклонения блока.**
1. Отклонение от К-15 названо явно (ADR-126 §7, образцы ADR-072 §7 и ADR-125 §7): проверка 6b
   включается тем же коммитом, до апрува Owner. Основание — она не вводит нового правила, а даёт
   исполнителя правилу, действующему с ADR-007.
2. ПЕРВАЯ ЗАЯВЛЕННАЯ ЦИФРА BLAST RADIUS БЫЛА НЕВЕРНА: в текст записи ушло «17 совпадений в шести
   файлах», фактический прогон дал 172 в 46. Ошибка поймана прогоном до коммита и исправлена в
   самой записи, а не замолчана: поиск был выполнен, но его вывод в текст не перенесён — нарушение
   К-1 в чистом виде, в записи о форме.
3. Наслоение в ячейках очереди роудмапа не правилось: заведено строкой открытого вопроса
   (ADR-126 §6), другой носитель и другой объём.

`bash tools/hooks/selftest.sh` → пройдено 88, провалено 0. `bash tools/verify.sh` → GREEN.
`git commit --no-verify` не применялся.

## Блок 5 · Отправка F4-16 внесена в состояние (2026-08-11)

**Факт от Owner, дословно:** «документы из F4-16 я отправил вчера 10 августа».

**Что изменилось по существу.** Стоп ADR-095 §1 снят фактом отправки — снятие и было решением
Owner, оно исполнено действием. Критический путь ADR-095 §5 пройден до конца: карта → ревью →
учёт → документы → отправка. Следующий шаг сместился со строки 4 очереди на строку 14 (`F4-19`,
свагер Битрикса), она по своей пометке идёт перед строкой 5 (`F4-17`, прогон).

**Что НЕ установлено и потому не записано фактом.** Какая редакция документа Битриксу ушла:
`reference/2026-08-09_bitrix_flow_map_rev9.md` или `reference/2026-08-10_bitrix_flow_map_rev10.md`.
Обе существовали на дату отправки — rev10 внесена коммитом 6847d55 того же 10 августа. Разница
не косметическая: редакция 10 правила служебные числа и имя парного файла (ADR-121 §2, §4), и от
ответа зависит, на что ссылаться при разборе ответа подрядчика и какие ключи `Q-NN` считать
стабильными (ADR-115). Догадка сюда не подставлена (anti-improvisation), заведена строка
открытого вопроса и задан вопрос Owner.

**Замечено сверх предмета и заведено строкой.** Операция класса B выполнена 10 августа, а записи
в репо не получила до 11-го: механизма, который требует фиксации класса B, в проекте нет —
отправка и `git push` держатся на памяти человека ровно так, как К-9 держалась до появления хука.
Настоящей сессией не чинится: это отдельная запись и другой носитель.

`bash tools/hooks/selftest.sh` → пройдено 88, провалено 0. `bash tools/verify.sh` → GREEN.

## Блок 6 · Отправленный комплект установлен (2026-08-11)

**Ответ Owner, дословно:** «rev10».

**Вывод: расхождения между решением и действием нет.** Ушла пара
`reference/2026-08-09_listkg_flow_map_rev9.md` плюс `reference/2026-08-10_bitrix_flow_map_rev10.md`
— ровно та, что назначена ADR-121 §1. Это снимало главный риск вопроса: если бы ушла редакция 9
документа Битриксу, у подрядчика на руках оказался бы текст со служебными числами и именем
парного файла от прежней редакции (ADR-121 §2, §4), и разбор ответа пришлось бы вести по
документу, который в репо уже заменён.

**Следствие, которое теперь можно записать твёрдо.** Ключи `Q-NN` обоих документов закреплены
датой 2026-08-10: ADR-115 привязывает их стабильность к моменту отправки, а до сегодняшнего дня
момент не был зафиксирован. Разбор ответов подрядчиков ведётся по этим ключам и по этой паре.

**Строка вопроса закрыта в день заведения** и перенесена в `docs/00_STATE_ARCHIVE.md` дословно
(К-19). Вторая половина той же строки — отсутствие механизма, требующего фиксации операций
класса B записью, — осталась открытой: она вопросом Owner не закрывается и чинится отдельной
записью. Правило-кандидат сформулировано в самой строке: операция класса B закрывается тем же
коммитом, что и строка состояния о ней.

**Отклонение блока: правка сломала файл, гейт поймал.** Скриптовый вырез закрытой строки
захватил заголовок `## Блокеры` — срез искал следующую строку списка и нашёл её уже за границей
раздела. `tools/verify.sh` дал RED с точной причиной, раздел восстановлен ДО коммита. Это второй
за сессию случай, когда гейт поймал дефект, которого роль не заметила; первым был SIGPIPE в
собственной проверке (ADR-126 §5.1).

**Второе отклонение, поймано после коммита блока 6.** Эта заметка не попала в файл сессии с
первого раза: проверка в скрипте правки не сошлась, скрипт упал, а коммит всё равно ушёл —
команды стояли отдельными строками, и падение первой вторую не остановило. Описание коммита
отклонение несло, файл сессии — нет; дописано следующим коммитом. Урок ровно тот же, что
собирает вся эта сессия: связка «упало — значит дальше не идём» существует, только если её
кто-то держит.

`bash tools/verify.sh` → GREEN. `bash tools/hooks/selftest.sh` → пройдено 88, провалено 0.
