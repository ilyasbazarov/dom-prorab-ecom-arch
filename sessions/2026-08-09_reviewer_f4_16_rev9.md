# SESSION · 2026-08-09 · Reviewer (Claude Code, Fable) · reviewer_f4_16_rev9

**SHA сессии:** `3b02d9c97bc4796b605c6413bc8f67e48acabccd`
**Проверка признака пути (дословный вывод):** `git rev-parse HEAD` →
`3b02d9c97bc4796b605c6413bc8f67e48acabccd`; `date` → `Sun Aug  9 21:25:53 +06 2026`;
`git status --porcelain` — пусто. Самотест хука: «selftest: пройдено 46, провалено 0
(локали: C, en_US.UTF-8)».
**Предмет (адреса предъявлены до чтения обязательного контекста, К-36):**
`reference/2026-08-09_bitrix_flow_map_rev9.md:968-1104` (раздел 4.4 … текст ответа службы),
`:985-986` (ключ идемпотентности — идентификатор заказа на стороне сайта), `:1583` (5 из 5 по
CQ-35), `reference/2026-08-09_listkg_flow_map_rev9.md:853` (служебная черта),
`docs/02_ADR_LOG.md:7050` (ADR-120).
**Точка ревью:** ОДИН ПОЛНЫЙ ЗАХОД на дельту редакции 6 → 9 ОБОИХ документов комплекта F4-16
(ADR-113 §2, предмет сдвинут ADR-118 §3 и ADR-120 §5). Базы: `bitrix_rev6` (черта 883, текст
наружу 1..882), `listkg_rev6` (черта 724, текст наружу 1..722). Предмет: `bitrix_rev9` (черта
1238, текст наружу 1..1236), `listkg_rev9` (черта 853, текст наружу 1..851). Это последний гейт
содержания (ADR-024); после него — снятие стопа решением Owner (ADR-095 §1), класс B, мандатом
не покрыт.

## 1. Дельта 6 → 9: отнесение по классам, свой прогон diff

Документ Битриксу, `diff -u` текста наружу 1..882 против 1..1236: **18 ханков**. Отнесение:
- класс «дефект моста сборки» (ADR-112 §1): ханки в таблице 2.1 (строки стадий 4, 7, 10), в 2.3
  (оговорка дублёра), шаги `OF-18`, `OF-19`, `OF-24`, раздел 3.1, раздел 4.3;
- класс «статусный контур + сводка вопросов» (ADR-112 §2-§4): раздел 0, вставка 2.4-2.8,
  переименование 2.4 → 2.9, «Вопрос по этому месту» в 5.2, ссылка «раздел 2.9» в разделе 6,
  раздел 7;
- класс «требования F4-20» (ADR-116): строка запрета в 2.2, шаги `OF-01` (пять подвопросов
  CQ-35), `OF-02`, `OF-03`, `OF-20`, вводный абзац раздела 4, разделы 4.4-4.6, строки
  `Q-13`…`Q-16`;
- класс «ключ идемпотентности» (ADR-118 §2): внутри раздела 4.4 (правило 2, строка таблицы
  исходов, абзац «выход, а не вход», признак приёмки);
- дата в шапке (К-29).
**Ханков вне пяти заявленных классов: 0.** Промежуточные числа воспроизведены дословно:
дельта 8 → 9 — 3 ханка, удалено 4, добавлено 13, все в разделе 4.4 (заявлено ADR-118 §3);
дельта 7 → 8 — 9 ханков, удалена 21, добавлено 192 (заявлено ADR-116 §6).

Документ List.kg, `diff -u` текста наружу 1..722 против 1..851: **8 ханков** — 7 ханков дельты
6 → 7 (класс 1 следствие дефекта: 2.3, `OF-18`, 4.2; класс 2 статусный контур: раздел 0, вставка
2.4-2.6, два абзаца в 3.8, ссылка «раздел 2.7» в разделе 6, раздел 7) плюс 1 ханк даты в шапке.
**Ханков вне классов: 0.** Дельта 8 → 9 — **0 строк** расхождения; дельта 7 → 9 — **1 ханк**,
только строка даты (заявлено ADR-120 §6). Требований F4-20 документ List.kg не получил — верно
по ADR-113 §3.

## 2. Проверки, названные пусковым сообщением сверх перечня промта

1. **Два числа ADR-115 §4 — воспроизведены прогоном, не чтением.**
   `awk '/^\*\*OF-01 ·/,/^\*\*OF-02 ·/' | grep -cE '^  [0-9]\. '` → **5** (подвопросы CQ-35 в
   шаге `OF-01`, `bitrix_rev9:268-276`). Строк раздела 7 без формулировки по названному адресу —
   **0**: все десять сверены поимённо (`Q-13`→OF-01 :264-276; `Q-14`→4.4 :1029; `Q-15`→4.5
   :1087; `Q-16`→4.6 :1116; `Q-17`→3.5 :896; `Q-18`→5.2 :1159; `Q-19`…`Q-22`→раздел 6 :1173,
   :1182, :1186, :1199). У документа List.kg все двенадцать строк раздела 7 также разрешаются
   (Q-01→3.3 :524-528; Q-02→OF-25 :362-363; Q-03→3.8 :644-649; Q-04→OF-33 :443-447 и р.6
   «Пятое» :792; Q-05→р.6 «Четвёртое» :776-790 и OF-07 :261-266; Q-06→3.4 :543-544; Q-07→4.1
   :675-676; Q-08…Q-12→раздел 6).
2. **Двусоставность класса Б (ADR-111 §5):** разделы 4.4, 4.5, 4.6 несут И строку «По какому
   признаку мы это увидим» (`:1014`, `:1081`, `:1111`), И строку «Что мы просим сказать по этому
   пункту» (`:1029`, `:1087`, `:1116`). Требований без второй половины — 0.
3. **Согласованность статусного контура (ADR-112 §5), оба документа:** стадий по значению
   статуса **6** (4, 6, 7, 10, 12, 14) — прямая и обратная таблицы дают одно множество (bitrix
   2.6/2.7, listkg 2.3/2.5); стадий без значения **8**; сумма **14** = перечень 2.1/2.4 = канон
   §3.4 (`canonical_order_flow.md:121,144-151` — ветвление «Собран» в каноне есть, Track A
   исполнен); значений без стадии **0**; пустых клеток «статус в этот момент» **0**.
4. **Внутренние коды выше черты (ADR-102 §3, дословная команда, локаль К-35):** **0 и 0**
   (прогон по границам 1..1237 и 1..852 — накрывает весь текст наружу).
5. **Слой переписки выше черты, включая будущее время (ADR-108 §5 в редакции ADR-109 §3):**
   **21** совпадение (bitrix) — 15 ссылок на выданное задание «задача N пакета от 3 августа» +
   6 ложных (`:435`, `:550` письменный ответ кладовщика; `:776` клиент прислал; `:860` 1С
   присылает; `:766`, `:1079` «обещать клиенту»); **19** (listkg) — 14 ссылок на задание + 5
   ложных (`:338`, `:444`, `:639`, `:682`, `:833`). Ссылок на наши письма **0 и 0**, обещаний
   будущей присылки **0 и 0**. Числа совпали с вердиктами служебных частей построчно.
6. **Связка порядка — пересчитана своим сплошным прогоном, а не сверкой с числом (ADR-116 §6):**
   форма прогона служебной части по 1..1236 даёт **26** совпадений; порядко-зависимых
   утверждений **ВОСЕМЬ** (раздел 0 «Ключ шага» :13-14; 2.4 вводный :135-136; раздел 3 :785;
   `OF-08` :391; 3.4 :878-879; 4.3 :965; 5.2 :1150; раздел 7 вводный :1213-1214). Девятого нет:
   ни одно из требований 4.4-4.6 и ни один переписанный шаг на подрядчика по 1С и парный
   документ не ссылаются. Встречного ограничения документ List.kg не создаёт (`l9:14-15`,
   `:166-167`, `:825-826` — форма «тем же комплектом»).
7. **INV-4, внутренний номер 1С в клиентских точках:** вхождений **0**. Шаблоны раздела 4.5
   несут `{{1}}` = `UF_CLIENT_ORDER_NO` с явным «НЕ номер документа 1С» (`:1057`, `:1062-1064`);
   `OF-29` требует замену `purchase_order_number` на клиентский номер (`:685-689`); приёмка 4.1
   закрепляет «ни в одной клиентской точке… внутренний номер» (`:924-925`).
8. **Разъём Д-13 (К-41, ADR-114 §5):** прогон `Wazzup|Meta|Business Manager|WABA|модерац|
   бизнес-портфолио|провайдер` по 1..1236 — **0** совпадений; наружу ушла только половина
   подрядчика (раздел 4.5).
9. **Номер шаблона `#19` (ADR-116 §3):** реестр подтверждён — `d4_t07:76` «Шаблоны \#12
   (tracking) и \#17 (waitlist) исключены», `:74,586` — `#18` занят `payment_pending_reminder`;
   вхождений `#19` в реестре нет, номер свободен. Раздел 4.5 воспроизводит это дословно.
10. **Ключ идемпотентности (ADR-118 §3, три числа):** мест «ключ = клиентский номер» — **0**;
    мест «ключ = идентификатор заказа сайта» — **2** (правило 2 :985-986, строка таблицы :1009);
    утверждений «сайт располагает номером до первого успешного вызова» — **0** (:988-990 прямо
    утверждает обратное). Реализация совпадает с ADR-118 §2 и подтверждением Owner (ADR-119 §2).
11. **Числа приёмки служебных частей — воспроизведены:** bitrix: строк шага 36, разделов шага
    2.9 — 36, подразделов р.4 — 6, подразделов р.2 — 9, строк «Проверка:» — 4, строк `Q-NN` —
    10, строк 2.7 — 14, строк 2.8 — 9; listkg: строк шага 36, разделов шага 2.7 — 14, строк
    «Проверка:» — 5, строк `Q-NN` — 12, строк 2.4 — 14, 2.5 — 14, 2.6 — 9, кириллических
    `1С-B1` — 0, подразделов р.2 — 7, р.3 — 8. Ссылок на `ADR-117` и на `bitrix_rev8` в
    `listkg_rev9` — по одной, обе в блоке «Что изменено против редакции 8» (`l9:882`, `:888`) —
    описывают исправляемое (заявлено ADR-120 §6).

## 3. Таблица находок

| № | Уровень | Что не так | Цитата | Чем грозит | Кому |
|---|---|---|---|---|---|
| 1 | FIX | Приёмка служебной части документа Битриксу объявлена «перепрогнана по редакции 9», но граница текста наружу в ней осталась от редакции 8: «Текст наружу — строки 1..1227» при черте **1238** (текст наружу 1..1236 — так говорит и пусковое сообщение, и сама часть строкой про diff «редакции 9 (1236 строк)»); все четыре дословные команды приёмки прогнаны с `head -n 1227` — девять строк текста наружу (1228-1236: «Вопросов десять…», подпись) в заявленные прогоны не попали. Мой перепрогон по полной границе 1..1236 даёт те же числа (кодов 0, слой переписки 21, связка 26/8, Д-13 0) — содержательного расхождения нет. Та же строка несёт обрывок двойной скобки «(была 1229)(была 1058)» | `reference/2026-08-09_bitrix_flow_map_rev9.md@3b02d9c:1561-1562` против `:1238` и `:1565`; команды `:1613`, `:1619-1620`, `:1628-1629`, `:1666-1667` | приёмка не воспроизводится дословной командой из файла: взявший константу 1227 (следующее ревью, скрипт M1-12) получит другой срез и другое основание чисел; сам класс — «правка без пересчёта констант» — тот же, что находки №1-№2 захода rev4 (ADR-109 §1) | Architect / Owner |
| 2 | FIX | Блок «Отправка» служебной части того же файла называет гейтом «заход Reviewer на дельту редакции 6 → 8 (ADR-113 §2, полный)» — предмет сдвинут на 6 → 9 ТОЙ ЖЕ записью, которой выпущен сам файл (ADR-118, blast radius: «предмет ADR-113 §2 сдвигается с редакции 8 на 9»), и затем на «6 → 9 обоих» (ADR-120 §5) | `reference/2026-08-09_bitrix_flow_map_rev9.md@3b02d9c:1657-1659` против `docs/02_ADR_LOG.md@3b02d9c:6980` и `:7103-7107` | провенанс отправляемого файла называет несуществующий гейт; при сверке условий отправки перед снятием стопа строка отсылает не туда | Architect / Owner |
| 3 | NOTE | Пояснение к перечню связки порядка: «порядко-зависимых утверждений среди них ВОСЕМЬ (два из них занимают по две строки)» — по моему прогону двухстрочных из восьми ЧЕТЫРЕ: :13-14, :135-136, :878-879, :1213-1214. Оба числа, входящие в приёмку (26 совпадений, 8 утверждений), воспроизведены; неточна только скобка | `reference/2026-08-09_bitrix_flow_map_rev9.md@3b02d9c:1668-1669` против прогона (совпадения на обеих строках каждой из четырёх пар) | арифметика «строки = утверждения + остальные» по скобке не сходится (10 ≠ 12 из 26); на вердикт и числа приёмки не влияет | Architect |

Все три находки лежат в служебной части документа Битриксу, НИЖЕ черты: текст наружу ни одна не
затрагивает. Файл — снимок (К-33): закрытие — либо новой редакцией служебной части, либо явное
принятие решением Owner с фиксацией.

## 4. Проверено и чисто (сводно)

Классы дельты разложены без остатка своим diff (§1); оба числа ADR-115 §4 воспроизведены
прогоном (§2.1); двусоставность 4.4-4.6 выполнена (§2.2); статусный контур 6+8=14, 0, 0 в обоих
документах и совпадает с каноном §3.4 (§2.3); внутренних кодов 0/0 (§2.4); слой переписки —
ссылок на наши письма и обещаний будущей присылки 0/0 и 0/0 при 21 и 19 совпадениях с вердиктом
по каждому (§2.5); связка порядка — восемь, пересчитано своим прогоном, девятого нет (§2.6);
INV-4 держится (§2.7); разъём Д-13 — 0 упоминаний нашей половины (§2.8); `#19` свободен и
доказан реестром (§2.9); ключ идемпотентности исполним, три числа сходятся (§2.10); реализация
решений ADR-112, ADR-113 §3, ADR-114 §5, ADR-115, ADR-116, ADR-118 §2, ADR-119 §2, ADR-120 §1
расхождений с записями не имеет. Два заранее названных открытых места (асимметрия имени пары;
ADR-117 proposed плюс две строки дефектов параллельного контура) предъявлены и новостей не дали:
текст наружу редакций 7, 8, 9 документа List.kg совпадает построчно, кроме строки даты, — мой
diff подтверждает (0 строк против rev8, 1 ханк против rev7).

## 5. ВЕРДИКТ

**Отправлять можно**: находок уровня STOP нет, гейт ADR-024 на дельте 6 → 9 пройден. Судьба
находок №1-№3 (все ниже черты) — решение Architect и Owner; отправку они не запирают, но №1-№2
стоит закрыть до отправки, пока цена правки — служебная часть, а не разъезд провенанса.
Отправка — класс B: снятие стопа ADR-095 §1 остаётся решением Owner и этой сессией не
затрагивается.

## 6. Замечено рядом, вне предмета ревью (scope не расширяю)

- `l2-registers/contractor_questions.md@3b02d9c:37,38,41` — строки CQ-31, CQ-32, CQ-35 называют
  носителями разделы `reference/2026-08-09_bitrix_flow_map_rev8.md`, тогда как отправке подлежит
  редакция 9; для CQ-31 названный носитель «раздел 4.4 rev8» несёт неисполнимый ключ, исправленный
  редакцией 9. Реестр в предмет захода не входит; правка адресов — Architect.
- Две строки `docs/00_STATE.md` держались «до захода Reviewer» (обязательство ADR-115; развилка
  Р-9): заход проведён, условие исполнено — перенос в архив и закрытие остаются за
  Architect/Owner, аннотации «Обновление» внесены этой сессией.
