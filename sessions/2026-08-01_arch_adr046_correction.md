# FILE: sessions/2026-08-01_arch_adr046_correction.md

- Задача: корректирующая сессия по находкам Reviewer-сессии reviewer_adr044_045 (STOP №1,
  FIX №2, FIX №3, NOTE №4); определить форму закрытия для уже закоммиченной ADR-045.
- Сделано: SHA сессии 37ba6ed9, дерево чистое. Форма закрытия установлена механически, а не
  выбором: тело закоммиченной записи править нельзя — ADR-016
  (docs/02_ADR_LOG.md@37ba6ed9:181-183) и хук tools/hooks/pre-commit@37ba6ed9:39-47
  пропускают только строки статуса, исключения для proposed нет, К-15 подтверждает.
  Следовательно корректирующая запись, прецедент ADR-043. STOP №1 снят без правок:
  Reviewer работал на SHA 5e69b8af, ADR-045 внесена следующим коммитом 184c6af (на 5e69b8af
  grep даёт 0 совпадений, на 184c6af — есть); на HEAD запись лежит на :1073-1133, строка
  индекса — docs/02_ADR_INDEX.md@37ba6ed9:55. FIX №2 подтверждён пересчётом: каталогов 15
  рекурсивно, 12 без l3-external, 11 верхнего уровня; 18 не даёт ни одно прочтение, при этом
  109 файлов сходится точно. При перепроверке той же аттестации найдены два дефекта, не
  названные Reviewer: docs/09_RUNBOOK.md@5e69b8af:41 (раздел «## 2. Признак пути и SHA») —
  совпадение понятия без вердикта в blast radius, содержательное, поскольку тело раздела
  :43-49 излагает признак прежней отрицательной логикой и на апруве устаревает; и ложная
  ссылка sessions/2026-07-31_arch_m1_correction.md:46 — искомых понятий там нет, реальное
  совпадение :21 и оно про «признак исполнения», другое понятие. §6 ADR-044 проверен
  симметрично и чист: 41 маркер пяти форм воспроизводится точно (19+15+4+1+2), три
  совпадения слова «маркер» названы с верными вердиктами. FIX №3 подтверждён: признак
  ADR-045 :1131-1133 упражняется только веткой «продолжить», ветка «стоп» штатным запуском
  не наступает никогда; та же форма у ADR-041 :904-905. NOTE №4 подтверждён: [пачкой …] = 15,
  не 14; при 14 сумма пяти форм даёт 40 против заявленного 41 в том же абзаце; обе цифры в
  §Контекст, §Решение и blast radius не затрагивают, суждение верно и при 15. Сплошной поиск
  К-27 по понятию «признак исполнения» проведён на полном дереве 37ba6ed9 (111 файлов, из них
  78 вне l3-external), 31 совпадение, вердикт по каждому — в blast radius ADR-046. Замеры
  положены файлом reference/2026-08-01_adr044_045_recount.md (К-22).
- Отклонения: пусковое сообщение чатового пути исполнено внутри Claude Code — третий случай
  подряд. Признак сработал наполовину: роль назвала несовпадение первым абзацем и продолжила
  работу, то есть «упомянула» вместо «остановилась»; это фактура к FIX №3, заведена строкой
  STATE. Архитекторская сессия внутри Claude Code — разовое исключение мандата (CLAUDE.md
  §Мандат), продолжение строки STATE от 2026-08-01. Файлы не правились, коммит не делался.
  Отдельно: git grep -i не сворачивает регистр кириллицы — первый прогон поиска дал 4
  совпадения против 31 корректных, без сообщения об ошибке; прогон перевыполнен явным
  классом (П|п). Заведено строкой STATE как угроза К-26/К-27/К-28.
