# SESSION · 2026-08-11 · Architect режим B (Claude Code, Opus) · arch_f4_17_brief

**SHA сессии:** `49a0954fb54a83faa92c3287d8fd16f338239c22` (`git rev-parse HEAD` на старте, дерево
чистое). Самотест хуков: пройдено 88, провалено 0. Пуск —
`reference/2026-08-11_arch_f4_17_brief_launch.md`, собран сессией `arch_rev11_findings`, блок 5.

## 1. Задача

Выпустить `briefs/F4-17.md` по `docs/06_BRIEF_TEMPLATE.md` — бриф на прогон шестнадцати шагов
карты, оставшихся в задаче после сужения ADR-134 §1. Больше сессия не делает ничего: прогон
исполняет Executor по брифу, регистр гэпов заполняет он же.

## 2. Предъявление предмета (К-36)

Все одиннадцать адресов пуска предъявлены до чтения обязательного контекста и найдены. Пуск
собран на коммит `dabad2d`, HEAD сессии — `49a0954`; расхождение проверено и безопасно: коммит
`49a0954` тронул только `reference/2026-08-11_arch_f4_17_brief_launch.md`,
`sessions/2026-08-11_arch_rev11_findings.md` и `sessions/JOURNAL.md`, ни один адресуемый файл в нём
не менялся, номера строк не сдвинулись.

## 3. Что решено (полностью — ADR-135, `proposed`)

Четыре вопроса, отданных сессии брифа записью ADR-134 §5, решены ролью; решения Owner ни по одному
не было, форма объявлена прямо (ADR-117 §3, К-40).

1. **Каналы.** Ни один не вынесен вне-scope, все шестнадцать шагов остаются в объёме. Вопрос
   ADR-134 §5 склеивает два разных: реализацию подрядчика устанавливает сам прогон, поэтому она
   вопросом объёма быть не может; наш доступ есть предусловие, и по нему в репо лежит один
   установленный факт (портал Битрикса, `l2-registers/open_questions.md@49a0954:30`) и ни одного
   адреса, объявляющего Яндекс, Wazzup или витрину CMS недоступными. По «не установлено» шаг
   вынести запрещает К-14. Доступ стал первым шагом брифа, его отсутствие — записанной причиной.
2. **Разбивка.** Пять файлов по прогонам: 8 + 2 + 1 + 3 + 2 = 16. Один заказ не бывает разом
   доставкой и самовывозом, оплаченным и неоплаченным; три сценария оставляли без места `OF-05` и
   ветку мутации состава.
3. **Приёмка.** Утвердительные числа: 16 из 16, 5 файлов, 4 из 4 точек доступа плюс одиннадцать
   нулей (К-28).
4. **Безопасность.** Отдельный шаг нужен: ADR-104 §7 писался про боевую 1С, а у половины без 1С
   свои необратимые края — реальная заявка Яндекса и реальные сообщения клиенту. `OF-20` отнесён к
   классу B правилом сомнения.

## 4. Найдено сверх задачи

Граница ADR-134 §2 проведена по полю «Направление», а источник предусловия шага — другая вещь.
В объёме оказались шаги, чей триггер лежит вне объёма: `OF-11` и `OF-19` входят стадией после
`success` `/create_order`, `OF-08` запускается исходом `partial`, `OF-15` — успешным ответом
`/update_order`, `OF-29` — стадией «Успешно реализовано». Дефектом решения это не является и
объёма не меняет: закрыто правилом трёх способов предусловия с запретом имитировать ответ 1С
подложным вызовом (ADR-135 §3).

Попутно исправлен отставший счёт роудмапа: «Из семи выпущены» читать как «Из девяти» — задачи
`F4-22` и `F4-23` заведены позже абзаца, а число при них не пересчитали.

## 5. Отклонения

Нет. Карта, канон, `l3-external` и регистры сессией не тронуты ни строкой: бриф их только
адресует.

## 6. Блок 2 — замер готовности по требованию Owner

Owner вернул работу ADR-135: установление доступа было отложено на первый шаг задачи вместо того,
чтобы быть собранным сессией. Замер собран и положен файлом
`reference/2026-08-11_f4_17_run_readiness.md`.

Итог: готовых позиций 1 из 5, шагов объёма, проверяемых сегодня, 0 из 16. Посылка ADR-135 §1
опровергнута двумя адресами — строка `F4-21` роудмапа держит неподключённый канал WhatsApp, а
воронка из 14 стадий не подтверждена заведённой (снимков ноль, строка 13 очереди открыта). Воронка
оказалась гейтом прогона, а не параллельной дорожкой: без неё не работает и ручной способ
предусловия ADR-135 §3. Витрина CMS и Яндекс остаются не установленными ни в одну сторону (К-14).

Развилка об объёме `F4-17` подана Owner тремя вариантами; запись, заменяющая ADR-135, выпускается
после его ответа. Тело ADR-135 не правится (ADR-016), статус остаётся `proposed`.

## 7. Блок 3 — решение Owner по развилке Р-1, вариант В

Owner ответил дословно: «Р-1 вариант В». Применено записью ADR-136 (`accepted`, секция «Решение
Owner» с цитатой).

Воронка объявлена гейтом прогона: строка 13 предшествует `F4-17` и `F4-24`, формулировка ADR-100 §3
о единственной параллельной строке сужена. `F4-17` сужена второй раз — восемь шагов внутри CRM;
восемь шагов через внешние каналы заведены строкой 18 задачей `F4-24`, 8 + 8 = 16 сходится.

Бриф вышел редакцией 2: гейт проверяется первым шагом по снимку воронки (14 / 0 / 0), пять файлов
протокола, первичных строк 2 + 2 + 1 + 2 + 1 = 8, строка `OF-19` полна только при обоих исходах
маршрутизации. Прогонная сделка создаётся руками — `OF-01` вышел из объёма вместе с витриной.

ADR-135 переведена в `superseded` правкой одной строки статуса (ADR-016): §1 снят, §2 переиздан,
§3-§5 оставлены в силе поимённо, чтобы отмена не забрала годное.
