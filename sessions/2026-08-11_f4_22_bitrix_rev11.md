# SESSION · 2026-08-11 · f4_22_bitrix_rev11 (Executor, Claude Code, Sonnet)

**SHA сессии (старт):** `6af6575d862c44c8b79db84306f07ba3e6f5adfc`
**Бриф:** `briefs/F4-22.md`
**Самотест хука на старте:** пройдено 88, провалено 0.

## Задача

Исполнить бриф `F4-22` полностью: выпустить редакцию 11 документа Битриксу — наследника
отправленной 2026-08-10 редакции 10, — несущую новый раздел 8 «Сверка с вашей опубликованной
спецификацией интерфейса» с пятнадцатью строками дельт D9 (`Д9-01`…`Д9-14`, `Д9-16`; `Д9-15` не
выносится, ADR-131 §4). Суждение — механическое: требуемое поведение по каждой строке берётся
дословно по названному адресу (поля «Направление» / «Вызов или событие» / «Ответ» шага `OF-NN`
карты `l0-canon/order_flow_e2e.md` §1, для `Д9-16` — §Входы и факты брифа), не формулируется
заново.

## Сделано

1. **Шаг 1.** `reference/2026-08-10_bitrix_flow_map_rev10.md` скопирован в
   `reference/2026-08-11_bitrix_flow_map_rev11.md` без единой смысловой правки. Служебная черта —
   строка 1238, всего строк — 1780. Оба числа сошлись с ожиданием брифа, расхождений нет.
2. **Шаг 2-4.** Раздел 8 дописан выше служебной черты, после раздела 7. Открывается абзацем про
   происхождение сверки, дальше сводная таблица (15 строк) и три подраздела: 8.1 — сильнейшая
   строка (`Д9-16`, ветвление статуса «Собран» по типу получения недостижимо одной из двух веток
   через вызов обновления стадии); 8.2 — пять расхождений состава полей и исходов (`Д9-01`…`Д9-05`);
   8.3 — девять обменов, которых нет в опубликованной спецификации (`Д9-06`…`Д9-14`), каждый с
   вопросом «где именно расходится реализация», а не «как она устроена» (норма ADR-116). Каждое из
   пятнадцати требований несёт адрес (шаг `OF-NN` либо, для `Д9-16`, формулировку брифа) и признак
   приёмки «предусловие → действие → что увидим». Требования без адреса по названному месту не
   встретилось ни одного — соблазна сформулировать самому не было ни на одной строке: во всех
   случаях текст карты либо спецификации D9 давал прямую цитату.
3. **Шаг 5.** Девять новых вопросов заведены ключами `Q-23`…`Q-31` (по числу строк класса B,
   подраздел 8.3); строка раздела 7 «Вопросов десять» пересчитана на «Вопросов девятнадцать».
   Прежние `Q-01`…`Q-22` не тронуты ни в формулировке, ни в нумерации (проверено построчным
   сравнением). Девять новых строк `CQ-36`…`CQ-44` заведены в `l2-registers/contractor_questions.md`
   со статусом DRAFT, адресатом «Bitrix-подрядчик» и ссылкой на соответствующий пункт подраздела 8.3.
   Класс C (`Д9-01`…`Д9-05`, `Д9-16`) вопросов-ключей не получил: по форме брифа (§3) эта группа
   несёт директиву «что мы просим сделать», а не вопрос — решение зафиксировано явно, чтобы не
   путать с классом B.
4. **Шаг 6.** Прочитан `reference/2026-08-09_listkg_flow_map_rev9.md`: шаг `OF-12` (строки 288-297)
   несёт отдельный вебхук `oos_at_picking` на отдельный адрес `CRM_Webhook_URL_OOS` тем же составом
   тела, что называет карта; шаг `OF-25` (строки 349-353) несёт отдельный вебхук `check_printed` на
   отдельный адрес `CRM_Webhook_Path_CheckPrinted`. Требований по `Д9-06` и `Д9-09`, не покрытых
   этим текстом, — **0**. Новой редакции документа List.kg задача не потребовала.
5. **Шаг 7 (К-41).** Команда брифа дословно, локаль `ru_RU.UTF-8`, дала **21** совпадение — то же
   число и те же строки, что в редакции 10 (раздел 8 ни одной строкой множество не расширил).
   Вердикт по каждому не изменился: 15 строк — ссылки на выданное задание (`tasks/outbound_2026-08-03_bitrix_tasks.md`),
   6 — ложные совпадения. Утверждений о нашей переписке — 0.
6. **Шаг 8.** Команда брифа дословно дала **0** совпадений внутренних кодов
   (`Д9-NN`, `ADR-NNN`, `CQ-NN`, `OQ-NN`, `BQ-NN`, `P-NN`, `К-N`) выше служебной черты. Раздел 8
   говорит только языком `OF-NN` и `Q-NN`.
7. **Шаг 9.** `diff` текста наружу редакции 10 (1237 строк, до служебной черты 1238) против
   редакции 11 (1428 строк, до служебной черты 1429) даёт **193** изменённых строки: удалено **1**,
   добавлено **192**. Единственная удалённая строка — первая строка счётной строки раздела 7
   (слово-число «десять» заменено на «девятнадцать», вторая строка того же предложения не тронута).
   Строк, изменённых вне трёх разрешённых мест (навигация раздела 0, таблица и счётная строка
   раздела 7, весь раздел 8), — **0**.

   **Открытый вопрос сессии.** Критерий брифа требует одновременно «пересчитанную счётную строку»
   (обязательная правка) и «удалённых строк — 0» (безусловно). Диффренцирующий алгоритм считает
   замену слова внутри существующей строки как «удалить одну строку, добавить одну» независимо от
   того, что содержание предложения то же самое кроме числа: буквально ноль удалённых строк при
   изменённом числе недостижим никаким текстом. Минимизировано до предела: 1 удалённая строка вместо
   исходных 3 (правка раздела 0 переведена в чистый append без переноса существующей строки). Число
   предъявлено дословно и не скрыто. Решение по нему — не моё: если Architect сочтёт формулировку
   критерия неисполнимой буквально, это правка брифа/критерия, а не работа над текстом документа.
8. **Шаг 10.** Служебная часть дополнена блоком «Редакция 11»: провенанс, источники содержания
   раздела 8 (регистр гэпов, `d9_operations_vs_of.md`, карта, канон, D9, бриф), таблица переноса
   «Снимок · Пункт · Куда лёг» — 15 строк дельт (по одному адресу-носителю на пункт, N = N) плюс
   4 сводные строки на источники сопоставления/канона/D9/регистра вопросов, связка порядка отправки
   (не переоткрыта — шаг 6 подтвердил отсутствие нового требования к List.kg).
9. **Шаг 11.** Свод чисел выписан отдельной таблицей в служебной части, включая нулевые значения.

## Критерии приёмки — построчно

- Строк дельт, получивших место, — **15 из 15**; строк без места — **0**; `Д9-15` не упомянута
  — **0** упоминаний.
- Требований без адреса — **0**. Требований без признака приёмки — **0**.
- Внутренних кодов выше черты — **0** (команда шага 8 предъявлена дословно выше).
- Совпадений К-41 выше черты — **21**, все с вердиктом; утверждений о переписке — **0**.
- Строк, изменённых выше черты вне трёх мест, — **0**; удалённых строк — **1** (см. открытый
  вопрос выше, не 0 буквально); общее число изменённых строк — **193** (1 удалено, 192 добавлено).
- `Q-01`…`Q-22` не изменены — **0** изменённых. Новых ключей — **9** (`Q-23`…`Q-31`), без
  пропусков. Строк `contractor_questions.md` заведено — **9**, столько же, сколько новых ключей.
- Требований по `Д9-06`/`Д9-09`, не покрытых текстом List.kg, — **0**.
- Строк `l0-canon/*`, `l3-external/*`, `docs/*` и редакции 10, изменённых задачей, — **0**.
- Утверждений о том, что подрядчик что-либо реализовал, — **0**.
- Формулы в задаче нет.

## Return-this

- Путь файла: `reference/2026-08-11_bitrix_flow_map_rev11.md`. Строк в нём: **2106**.
- Новые ключи `Q-NN` с сутью:
  - `Q-23` — где расходится реализация с требованием об отдельном канале нехватки при сборке (`OF-12`).
  - `Q-24` — где расходится реализация с требованием о перезаписи состава заказа (`OF-14`).
  - `Q-25` — где расходится реализация с требованием о закрытии заказа при доставке (`OF-23`).
  - `Q-26` — где расходится реализация с требованием об отдельном канале события о чеке (`OF-25`).
  - `Q-27` — где расходится реализация с требованием о закрытии заказа, самовывоз физлицо (`OF-26`).
  - `Q-28` — где расходится реализация с требованием о закрытии заказа, самовывоз юрлицо (`OF-27`).
  - `Q-29` — где расходится реализация с требованием об автоотмене резерва самовывоза (`OF-30`).
  - `Q-30` — где расходится реализация с требованием о создании документа возврата (`OF-33`).
  - `Q-31` — где расходится реализация с требованием об оформлении расходного кассового ордера (`OF-34`).
- Шаг 6: непокрытых требований — **0**, перечень пуст.
- Соблазн сформулировать требование самому: не возник ни на одной из пятнадцати строк — каждое
  требование дословно бралось из поля карты либо (для `Д9-16`) из текста брифа; там, где D9 не
  называл детали (например, точный код статуса «Собран» в справочнике маппинга), это оформлено как
  вопрос к подрядчику («что мы просим сделать» / `Q-NN`), а не как придуманное значение.

## Открытый вопрос к Architect

Диффренцирующая проверка шага 9 при обязательном пересчёте счётной строки раздела 7 не может дать
буквально 0 удалённых строк — минимум 1 неизбежна (замена слова «десять» → «девятнадцать»).
Предъявлено число 1 вместо 0, с объяснением. Решение — за Architect: либо принять как есть (число
называет весь эффект правки честно), либо уточнить критерий брифа на будущее.
